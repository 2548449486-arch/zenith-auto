<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no,viewport-fit=cover">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="theme-color" content="#03081a">
<title>Zenith Auto α · AI购车精算师</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Sora:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet">
<script>window.__clerkKey='pk_test_c3RlcmxpbmctcGlwZWZpc2gtOS5jbGVyay5hY2NvdW50cy5kZXYk';</script>
<script async crossorigin="anonymous" src="https://cdn.jsdelivr.net/npm/@clerk/clerk-js@5/dist/clerk.browser.js"></script>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;-webkit-tap-highlight-color:transparent}
:root{
  --bg:#03081a;--bg2:#06101f;
  --s1:rgba(255,255,255,0.05);--s2:rgba(255,255,255,0.08);--s3:rgba(255,255,255,0.12);
  --b1:rgba(255,255,255,0.09);--b2:rgba(255,255,255,0.15);
  --gold:#f8af23;--gold2:#ffd166;
  --gd:rgba(248,175,35,0.1);--gg:rgba(248,175,35,0.05);
  --grn:#14cf8a;--grd:rgba(20,207,138,0.1);
  --red:#ff4757;--rdd:rgba(255,71,87,0.1);
  --amb:#ff9f43;--amd:rgba(255,159,67,0.1);
  --blu:#54a0ff;--bld:rgba(84,160,255,0.1);
  --pur:#a855f7;--prd:rgba(168,85,247,0.1);
  --tx:#dde3f8;--tx2:rgba(221,227,248,0.65);--mt:rgba(221,227,248,0.32);
  --fn:'Sora',-apple-system,sans-serif;--fm:'JetBrains Mono',monospace;
  --nh:60px;--hh:54px;--th:26px;--r:16px;
}
html,body{height:100%;overflow:hidden;background:var(--bg);color:var(--tx);font-family:var(--fn);-webkit-font-smoothing:antialiased;font-size:14px;-webkit-text-size-adjust:100%}
body::before{content:'';position:fixed;inset:0;z-index:0;pointer-events:none;background:radial-gradient(ellipse 120% 50% at 50% -10%,rgba(248,175,35,0.07),transparent 55%),radial-gradient(ellipse 80% 60% at 85% 90%,rgba(20,207,138,0.04),transparent 55%),linear-gradient(rgba(248,175,35,0.018) 1px,transparent 1px) 0 0/50px 50px,linear-gradient(90deg,rgba(248,175,35,0.018) 1px,transparent 1px) 0 0/50px 50px}
#app{position:fixed;inset:0;display:flex;flex-direction:column;z-index:1;background:var(--bg)}
#hdr{height:calc(var(--hh) + env(safe-area-inset-top));padding-top:env(safe-area-inset-top);display:flex;align-items:center;justify-content:space-between;padding-inline:13px;background:rgba(3,8,26,0.94);backdrop-filter:blur(40px);border-bottom:1px solid var(--b1);flex-shrink:0;z-index:60}
.logo{display:flex;align-items:center;gap:9px;cursor:pointer}
.logo-av{width:35px;height:35px;border-radius:50%;overflow:hidden;border:2px solid rgba(248,175,35,0.55);box-shadow:0 0 14px rgba(248,175,35,0.3);flex-shrink:0}
.logo-av img{width:100%;height:100%;object-fit:cover;object-position:center top}
.logo-n{font-size:14px;font-weight:700;letter-spacing:-.02em}
.logo-n em{color:var(--gold);font-style:normal}
.logo-s{font-size:8px;color:var(--mt);letter-spacing:.12em;font-family:var(--fm)}
.hdr-r{display:flex;align-items:center;gap:6px}
.lrow{display:flex;gap:3px}
.lp{background:var(--s1);border:1px solid var(--b1);border-radius:20px;padding:3px 7px;font-size:10px;font-weight:600;color:var(--mt);cursor:pointer;transition:all .18s;line-height:1}
.lp.on{background:var(--gd);border-color:rgba(248,175,35,.3);color:var(--gold)}
.uav{width:30px;height:30px;border-radius:50%;background:var(--s1);border:1px solid var(--b1);display:flex;align-items:center;justify-content:center;cursor:pointer;font-size:12px;overflow:hidden}
.uav svg{width:14px;height:14px;stroke:var(--mt);fill:none;stroke-width:1.8}
#ticker{height:var(--th);flex-shrink:0;background:rgba(248,175,35,0.028);border-bottom:1px solid rgba(248,175,35,0.07);overflow:hidden;display:flex;align-items:center}
.tt{display:flex;white-space:nowrap;animation:tick 55s linear infinite}
.ti{font-family:var(--fm);font-size:10px;color:var(--mt);padding:0 14px;display:flex;align-items:center;gap:4px}
.up{color:var(--grn)}.dn{color:var(--red)}.nt{color:var(--gold)}
@keyframes tick{from{transform:translateX(0)}to{transform:translateX(-50%)}}
#cnt{flex:1;overflow:hidden;position:relative;background:var(--bg)}
.sc{position:absolute;inset:0;overflow-y:auto;overflow-x:hidden;-webkit-overflow-scrolling:touch;padding:14px 13px calc(var(--nh) + env(safe-area-inset-bottom) + 16px);display:none;opacity:0;transform:translateY(8px);transition:opacity .25s,transform .25s;background:var(--bg);scrollbar-width:thin}
.sc.on{display:block}.sc.vis{opacity:1;transform:translateY(0)}
#sc-chat{display:none;flex-direction:column;overflow:hidden!important;padding:12px 13px 0!important;background:var(--bg)}
#sc-chat.on{display:flex}
#nav{flex-shrink:0;height:calc(var(--nh) + env(safe-area-inset-bottom));padding-bottom:env(safe-area-inset-bottom);background:rgba(3,8,26,0.96);backdrop-filter:blur(40px);border-top:1px solid var(--b1);display:flex;z-index:60}
.nb{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:3px;background:none;border:none;cursor:pointer;padding:6px 2px;position:relative}
.nb svg{width:19px;height:19px;stroke:var(--mt);stroke-width:1.6;fill:none;transition:all .22s}
.nb span{font-size:8.5px;font-weight:600;color:var(--mt);letter-spacing:.04em;transition:color .22s}
.nb::after{content:'';position:absolute;top:0;left:50%;transform:translateX(-50%);width:0;height:2px;background:var(--gold);border-radius:0 0 3px 3px;box-shadow:0 0 10px rgba(248,175,35,.6);transition:width .22s cubic-bezier(.34,1.56,.64,1)}
.nb.on svg{stroke:var(--gold);filter:drop-shadow(0 0 4px rgba(248,175,35,.5))}
.nb.on span{color:var(--gold)}
.nb.on::after{width:28px}
.stabs{display:flex;gap:6px;margin-bottom:14px;padding:3px;background:var(--s1);border-radius:12px;border:1px solid var(--b1)}
.stab{flex:1;padding:8px 4px;border-radius:9px;border:none;background:none;font-family:var(--fn);font-size:12px;font-weight:600;color:var(--mt);cursor:pointer;transition:all .2s;text-align:center}
.stab.on{background:rgba(248,175,35,0.12);color:var(--gold);border:1px solid rgba(248,175,35,.25)}
.stab-pane{display:none}.stab-pane.on{display:block}
.badge{display:inline-flex;align-items:center;gap:5px;background:var(--gd);border:1px solid rgba(248,175,35,.22);border-radius:20px;padding:3px 10px;margin-bottom:9px;font-size:10px;font-weight:600;color:var(--gold);letter-spacing:.1em;text-transform:uppercase}
.stitle{font-size:21px;font-weight:800;letter-spacing:-.03em;line-height:1.18;margin-bottom:5px}
.stitle .g{color:var(--gold)}
.ssub{font-size:11.5px;color:var(--tx2);line-height:1.6;margin-bottom:15px}
.card{background:var(--s1);border:1px solid var(--b1);border-radius:var(--r);padding:13px;margin-bottom:10px}
.seclbl{font-size:10px;font-weight:700;color:var(--mt);letter-spacing:.12em;text-transform:uppercase;display:flex;align-items:center;gap:8px;margin-bottom:10px}
.seclbl::after{content:'';flex:1;height:1px;background:var(--b1)}
.btn-g{display:flex;align-items:center;justify-content:center;gap:8px;width:100%;background:linear-gradient(135deg,var(--gold),var(--amb));border:none;border-radius:13px;padding:14px 20px;font-family:var(--fn);font-size:14px;font-weight:700;color:#020609;cursor:pointer;transition:all .18s;box-shadow:0 4px 22px rgba(248,175,35,.22)}
.btn-g:active{transform:scale(.97)}
.btn-o{display:flex;align-items:center;justify-content:center;gap:7px;width:100%;background:var(--s1);border:1px solid var(--b2);border-radius:12px;padding:12px 16px;font-family:var(--fn);font-size:13px;font-weight:600;color:var(--tx2);cursor:pointer;transition:all .18s}
.divider{height:1px;background:var(--b1);margin:12px 0}
.aitag{display:inline-flex;align-items:center;gap:4px;background:var(--prd);border:1px solid rgba(168,85,247,.25);border-radius:5px;padding:2px 6px;font-size:9px;font-weight:700;color:var(--pur);letter-spacing:.06em;vertical-align:middle;margin-left:4px}
.fi{animation:fi .35s ease both}.f1{animation-delay:.04s}.f2{animation-delay:.09s}.f3{animation-delay:.14s}.f4{animation-delay:.19s}.f5{animation-delay:.24s}
@keyframes fi{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:translateY(0)}}
.spin{display:inline-block;width:13px;height:13px;border:2px solid rgba(168,85,247,.2);border-top-color:var(--pur);border-radius:50%;animation:sp .8s linear infinite;vertical-align:middle;margin-right:5px}
@keyframes sp{to{transform:rotate(360deg)}}
input[type=text],input[type=email],input[type=password],textarea,select{width:100%;background:var(--s1);border:1px solid var(--b1);border-radius:11px;padding:11px 13px;font-family:var(--fn);font-size:13px;color:var(--tx);outline:none;transition:border-color .18s;-webkit-appearance:none;appearance:none}
input:focus,textarea:focus{border-color:rgba(248,175,35,.4)}
input::placeholder,textarea::placeholder{color:var(--mt)}
select option{background:#060c22}
#upzone{border:1.5px dashed rgba(248,175,35,.3);border-radius:var(--r);padding:24px 16px;text-align:center;cursor:pointer;transition:all .25s;margin-bottom:12px;position:relative;overflow:hidden;background:rgba(248,175,35,.018)}
#upzone:active{border-color:rgba(248,175,35,.6);background:rgba(248,175,35,.04)}
#upzone.scanning::after{content:'';position:absolute;inset:0;background:linear-gradient(180deg,transparent,rgba(248,175,35,.14) 50%,transparent);animation:beam 1.4s ease-in-out infinite}
@keyframes beam{0%{transform:translateY(-100%)}100%{transform:translateY(200%)}}
.upico{width:44px;height:44px;border-radius:13px;background:var(--gd);border:1px solid rgba(248,175,35,.22);display:flex;align-items:center;justify-content:center;margin:0 auto 10px}
.upico svg{width:21px;height:21px;stroke:var(--gold);stroke-width:1.5;fill:none}
.upt{font-size:13px;font-weight:600;margin-bottom:3px}.ups{font-size:11px;color:var(--mt)}
#fup{display:none}
.scanp{display:none;margin:10px 0}.scanp.on{display:block}
.scantr{height:3px;background:rgba(255,255,255,.18);border-radius:2px;overflow:hidden;margin-bottom:6px}
.scanf{height:100%;background:linear-gradient(90deg,var(--gold),var(--pur),var(--grn));border-radius:2px;width:0;transition:width .1s linear}
.scantxt{font-family:var(--fm);font-size:10px;color:var(--gold)}
.scanmod{font-family:var(--fm);font-size:9px;color:var(--pur);margin-top:2px}
#arep{display:none}
.savebanner{display:flex;align-items:center;justify-content:space-between;background:linear-gradient(135deg,rgba(20,207,138,.07),rgba(20,207,138,.025));border:1px solid rgba(20,207,138,.22);border-radius:var(--r);padding:13px 15px;margin-bottom:11px}
.savelbl{font-size:11px;color:var(--tx2);margin-bottom:3px}
.saveamt{font-family:var(--fm);font-size:24px;font-weight:700;color:var(--grn);line-height:1}
.astats{display:grid;grid-template-columns:1fr 1fr 1fr;gap:7px;margin-bottom:12px}
.astat{background:var(--s1);border:1px solid var(--b1);border-radius:12px;padding:10px 8px;text-align:center}
.asv{font-family:var(--fm);font-size:20px;font-weight:700;margin-bottom:3px}
.asl{font-size:9px;color:var(--mt)}
.arow{display:flex;align-items:center;gap:10px;padding:11px 12px;border-radius:12px;margin-bottom:6px;background:var(--s1);border:1px solid var(--b1);cursor:pointer;transition:all .18s;position:relative;overflow:hidden}
.arow.danger{background:var(--rdd);border-color:rgba(255,71,87,.22)}
.arow.warning{background:var(--amd);border-color:rgba(255,159,67,.22)}
.arow.ok{opacity:.55;cursor:default}
.arow:not(.ok):active{transform:scale(.98)}
.dot{width:7px;height:7px;border-radius:50%;flex-shrink:0}
.dot.danger{background:var(--red);box-shadow:0 0 8px var(--red)}
.dot.warning{background:var(--amb);box-shadow:0 0 8px var(--amb)}
.dot.ok{background:var(--grn)}
.rname{flex:1;font-size:12.5px;font-weight:500}
.arow.ok .rname{color:var(--tx2)}
.ramt{font-family:var(--fm);font-size:12.5px;font-weight:600}
.arow.danger .ramt{color:var(--red)}.arow.warning .ramt{color:var(--amb)}.arow.ok .ramt{color:var(--grn)}
.tcocars{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:12px}
.ccard{background:var(--s1);border:1px solid var(--b1);border-radius:14px;padding:11px}
.cclbl{font-size:9px;font-weight:700;letter-spacing:.12em;text-transform:uppercase;margin-bottom:6px}
.cclbl.a{color:var(--gold)}.cclbl.b{color:var(--grn)}
.csel{background:var(--s2);border:1px solid var(--b2);border-radius:8px;padding:7px 8px;font-family:var(--fn);font-size:11.5px;color:var(--tx);cursor:pointer}
.tbadge{margin-top:6px;font-size:9px;font-family:var(--fm);padding:2px 7px;border-radius:4px;display:inline-block}
.bev{background:var(--grd);color:var(--grn);border:1px solid rgba(20,207,138,.2)}
.bgas{background:var(--amd);color:var(--amb);border:1px solid rgba(255,159,67,.2)}
.bhyb{background:var(--bld);color:var(--blu);border:1px solid rgba(84,160,255,.2)}
.tparams{background:var(--s1);border:1px solid var(--b1);border-radius:14px;padding:13px;margin-bottom:12px}
.prow{margin-bottom:11px}.prow:last-child{margin-bottom:0}
.phdr{display:flex;justify-content:space-between;align-items:center;margin-bottom:6px}
.plbl{font-size:11px;color:var(--tx2)}
.pval{font-family:var(--fm);font-size:12px;color:var(--gold);font-weight:600}
.pslider{width:100%;height:4px;border-radius:3px;background:rgba(255,255,255,.18);-webkit-appearance:none;appearance:none;outline:none;cursor:pointer}
.pslider::-webkit-slider-thumb{-webkit-appearance:none;width:20px;height:20px;border-radius:50%;background:linear-gradient(135deg,var(--gold),var(--amb));box-shadow:0 0 12px rgba(248,175,35,.4),0 2px 8px rgba(0,0,0,.4);cursor:pointer}
.tcotots{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:12px}
.tcot{background:var(--s2);border-radius:12px;padding:11px;text-align:center}
.tcotl{font-size:9.5px;color:var(--mt);margin-bottom:4px}
.tcotv{font-family:var(--fm);font-size:19px;font-weight:700;line-height:1}
.tcotv.a{color:var(--gold)}.tcotv.b{color:var(--grn)}
.tmet{margin-bottom:13px}
.tmhdr{display:flex;justify-content:space-between;align-items:baseline;margin-bottom:6px}
.tmn{font-size:11px;color:var(--tx2)}
.tmvs{display:flex;gap:8px;font-family:var(--fm);font-size:11px}
.tmvs .va{color:var(--gold)}.tmvs .vb{color:var(--grn)}
.tbars{display:grid;grid-template-columns:1fr 1fr;gap:4px}
.tbarbg{height:5px;background:rgba(255,255,255,.05);border-radius:3px;overflow:hidden}
.tbarf{height:100%;border-radius:3px;transition:width .75s cubic-bezier(.23,1,.32,1)}
.tbarf.a{background:linear-gradient(90deg,var(--gold),var(--amb))}.tbarf.b{background:linear-gradient(90deg,var(--grn),var(--blu))}
.tverdict{background:linear-gradient(135deg,rgba(248,175,35,.065),rgba(20,207,138,.035));border:1px solid rgba(248,175,35,.22);border-radius:var(--r);padding:14px;margin-top:4px}
.tvlbl{font-size:9.5px;font-weight:700;color:var(--gold);letter-spacing:.12em;text-transform:uppercase;margin-bottom:7px}
.tvtxt{font-size:12.5px;line-height:1.7}
.tvtxt strong{color:var(--gold);font-weight:600}
.tvtxt .save{color:var(--grn);font-family:var(--fm);font-weight:700}
.loan-result{background:linear-gradient(135deg,rgba(84,160,255,.07),rgba(84,160,255,.025));border:1px solid rgba(84,160,255,.22);border-radius:var(--r);padding:14px;margin-top:4px}
.loan-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}
.loan-item-lbl{font-size:10px;color:var(--mt);margin-bottom:3px}
.loan-item-val{font-family:var(--fm);font-size:16px;font-weight:700;color:var(--blu)}
.loan-item-val.warn{color:var(--red)}
#chatmsgs{flex:1;overflow-y:auto;overflow-x:hidden;padding:4px 0 10px;min-height:0;-webkit-overflow-scrolling:touch}
.msg{margin-bottom:10px;animation:fi .25s ease both}
.msg.u{display:flex;justify-content:flex-end}
.msg.ai{display:flex;align-items:flex-end;gap:8px}
.chav{width:28px;height:28px;border-radius:50%;flex-shrink:0;overflow:hidden;border:1.5px solid rgba(248,175,35,.45);box-shadow:0 0 12px rgba(248,175,35,.3)}
.chav img{width:100%;height:100%;object-fit:cover;object-position:center top}
.bub{max-width:83%;padding:10px 13px;border-radius:16px;font-size:13px;line-height:1.62}
.msg.u .bub{background:linear-gradient(135deg,rgba(248,175,35,.14),rgba(255,159,67,.1));border:1px solid rgba(248,175,35,.22);border-bottom-right-radius:4px}
.msg.ai .bub{background:var(--s2);border:1px solid var(--b2);border-bottom-left-radius:4px}
.bub strong{color:var(--gold);font-weight:600}
.bub .mk{font-family:var(--fm);font-size:11.5px;color:var(--gold2)}
.bub blockquote{border-left:3px solid var(--gold);background:rgba(248,175,35,.05);padding:7px 11px;margin:7px 0;border-radius:0 8px 8px 0;font-style:italic;color:var(--tx2);font-size:12px}
.bub table{width:100%;border-collapse:collapse;margin:8px 0;font-size:11px}
.bub th{background:rgba(248,175,35,.1);color:var(--gold);padding:5px 7px;text-align:left}
.bub td{padding:5px 7px;border-bottom:1px solid var(--b1);color:var(--tx2)}
.tiertag{display:inline-flex;align-items:center;gap:3px;font-size:8.5px;font-family:var(--fm);padding:2px 6px;border-radius:4px;margin-bottom:4px}
.t-free{background:var(--grd);color:var(--grn);border:1px solid rgba(20,207,138,.2)}
.t-pro{background:rgba(248,175,35,.1);color:var(--gold);border:1px solid rgba(248,175,35,.2)}
.t-nego{background:var(--prd);color:var(--pur);border:1px solid rgba(168,85,247,.2)}
.dots{display:flex;gap:4px;padding:2px 0}
.dot2{width:6px;height:6px;border-radius:50%;background:var(--gold);opacity:.4;animation:dt 1.2s ease-in-out infinite}
.dot2:nth-child(2){animation-delay:.2s}.dot2:nth-child(3){animation-delay:.4s}
@keyframes dt{0%,80%,100%{opacity:.3;transform:scale(.8)}40%{opacity:1;transform:scale(1.1)}}
.chips{display:flex;gap:6px;overflow-x:auto;padding-bottom:9px;margin-bottom:2px;flex-shrink:0;scrollbar-width:none}
.chips::-webkit-scrollbar{display:none}
.chip{white-space:nowrap;background:var(--s2);border:1px solid var(--b2);border-radius:20px;padding:7px 13px;font-family:var(--fn);font-size:11px;color:var(--tx2);cursor:pointer;transition:all .18s;flex-shrink:0}
.chip:active{background:var(--gd);border-color:rgba(248,175,35,.3);color:var(--gold)}
.cinput{flex-shrink:0;padding:10px 0 calc(env(safe-area-inset-bottom) + 12px)}
.cinbox{display:flex;gap:8px;align-items:flex-end;background:var(--s2);border:1px solid var(--b2);border-radius:18px;padding:8px 8px 8px 14px}
#ci{flex:1;background:none;border:none;outline:none;resize:none;font-family:var(--fn);font-size:13px;color:var(--tx);line-height:1.5;max-height:86px;min-height:22px}
#ci::placeholder{color:var(--mt)}
.sbtn{width:36px;height:36px;border-radius:50%;flex-shrink:0;background:linear-gradient(135deg,var(--gold),var(--amb));border:none;cursor:pointer;display:flex;align-items:center;justify-content:center;transition:all .18s;box-shadow:0 0 12px rgba(248,175,35,.3)}
.sbtn svg{width:16px;height:16px;stroke:#02060e;stroke-width:2.2;fill:none}
.sbtn:active{transform:scale(.9)}
.nego-banner{background:linear-gradient(135deg,var(--prd),rgba(168,85,247,.05));border:1px solid rgba(168,85,247,.3);border-radius:14px;padding:13px 14px;margin-bottom:12px;display:flex;align-items:center;gap:12px}
.nego-av{width:44px;height:44px;border-radius:12px;background:var(--prd);display:flex;align-items:center;justify-content:center;font-size:20px;flex-shrink:0}
.nego-info-t{font-size:13px;font-weight:700;color:var(--pur);margin-bottom:2px}
.nego-info-s{font-size:11px;color:var(--tx2)}
.nego-btn{background:linear-gradient(135deg,var(--pur),#7c3aed);border:none;border-radius:10px;padding:9px 14px;font-family:var(--fn);font-size:12px;font-weight:700;color:#fff;cursor:pointer;margin-top:8px;display:inline-flex;align-items:center;gap:5px}
.rsel-wrap{display:flex;align-items:center;gap:8px;margin-bottom:14px}
.rsel-lbl{font-size:11px;color:var(--tx2);white-space:nowrap}
.rsel{flex:1;background:var(--s2);border:1px solid var(--b2);border-radius:10px;padding:9px 11px;font-family:var(--fn);font-size:12px;color:var(--tx);cursor:pointer}
.sig{border-radius:var(--r);padding:13px 14px;margin-bottom:12px;display:flex;align-items:flex-start;gap:12px}
.sig.buy{background:rgba(20,207,138,.07);border:1px solid rgba(20,207,138,.25)}
.sig.wait{background:rgba(255,159,67,.07);border:1px solid rgba(255,159,67,.25)}
.sig.nego{background:rgba(248,175,35,.07);border:1px solid rgba(248,175,35,.25)}
.sigico{font-size:26px;flex-shrink:0;line-height:1}
.sigtag{font-size:9px;font-weight:700;letter-spacing:.12em;text-transform:uppercase;margin-bottom:4px}
.sig.buy .sigtag{color:var(--grn)}.sig.wait .sigtag{color:var(--amb)}.sig.nego .sigtag{color:var(--gold)}
.sigtxt{font-size:12px;color:var(--tx2);line-height:1.65}.sigtxt strong{color:var(--tx)}
.drow{display:flex;align-items:center;padding:11px 12px;border-radius:11px;margin-bottom:6px;background:var(--s1);border:1px solid var(--b1);position:relative;overflow:hidden}
.drow.best{border-color:rgba(20,207,138,.3);background:rgba(20,207,138,.028)}
.drow.best::before{content:'';position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--grn)}
.drank{font-family:var(--fm);font-size:11px;color:var(--mt);width:18px;flex-shrink:0}
.dinfo{flex:1;min-width:0}
.dname{font-size:12px;font-weight:600;margin-bottom:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.dmeta{font-size:10px;color:var(--mt)}
.dprice{text-align:right;flex-shrink:0}
.dpv{font-family:var(--fm);font-size:13px;font-weight:700}
.drow.best .dpv{color:var(--grn)}
.ddisc{font-size:10px;color:var(--mt);margin-top:2px}
.ddisc.p{color:var(--grn)}.ddisc.n{color:var(--red)}
.airec{position:absolute;top:8px;right:8px;background:var(--grd);border:1px solid rgba(20,207,138,.3);border-radius:4px;padding:1px 6px;font-size:8px;font-weight:700;color:var(--grn)}
.tchart{display:flex;align-items:flex-end;gap:3px;height:48px;padding:0 3px;margin-bottom:5px}
.tbar-w{flex:1;display:flex;flex-direction:column;align-items:center;gap:2px;height:100%;justify-content:flex-end}
.tbar{width:100%;border-radius:3px 3px 0 0;background:rgba(248,175,35,.22);min-height:3px}
.tbar.hi{background:var(--red);opacity:.65}.tbar.lo{background:var(--grn)}.tbar.cur{background:var(--gold)}
.dealer-filters{display:flex;gap:6px;overflow-x:auto;padding-bottom:8px;margin-bottom:12px;scrollbar-width:none}
.dealer-filters::-webkit-scrollbar{display:none}
.df{white-space:nowrap;background:var(--s1);border:1px solid var(--b1);border-radius:20px;padding:6px 12px;font-size:11px;font-weight:600;color:var(--mt);cursor:pointer;transition:all .18s;flex-shrink:0}
.df.on{background:var(--gd);border-color:rgba(248,175,35,.3);color:var(--gold)}
.dealer-card{background:var(--s1);border:1px solid var(--b1);border-radius:16px;padding:14px;margin-bottom:10px;position:relative;overflow:hidden;cursor:pointer;transition:border-color .2s}
.dealer-card:active{border-color:var(--b2)}
.dealer-card.certified::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;background:linear-gradient(90deg,var(--gold),var(--amb))}
.dc-top{display:flex;align-items:flex-start;gap:10px;margin-bottom:10px}
.dc-logo{width:42px;height:42px;border-radius:12px;background:var(--s2);display:flex;align-items:center;justify-content:center;font-size:18px;flex-shrink:0}
.dc-info{flex:1;min-width:0}
.dc-name{font-size:14px;font-weight:700;margin-bottom:2px;display:flex;align-items:center;gap:6px}
.dc-cert{background:linear-gradient(135deg,var(--gold),var(--amb));color:#020609;font-size:8px;font-weight:800;padding:2px 6px;border-radius:4px;letter-spacing:.06em;flex-shrink:0}
.dc-brand{font-size:10.5px;color:var(--mt)}
.dc-rating{display:flex;align-items:center;gap:5px;margin-top:3px}
.stars{color:var(--gold);font-size:11px}
.dc-rv{font-size:10px;color:var(--mt)}
.dc-stats{display:grid;grid-template-columns:1fr 1fr 1fr;gap:6px}
.dc-stat{background:var(--s2);border-radius:8px;padding:7px;text-align:center}
.dc-stat-v{font-family:var(--fm);font-size:13px;font-weight:700;margin-bottom:1px}
.dc-stat-l{font-size:8.5px;color:var(--mt)}
.dc-tags{display:flex;flex-wrap:wrap;gap:5px;margin-top:8px}
.tag-ok{background:var(--grd);color:var(--grn);border:1px solid rgba(20,207,138,.2);font-size:9.5px;padding:3px 8px;border-radius:6px}
.tag-bad{background:var(--rdd);color:var(--red);border:1px solid rgba(255,71,87,.2);font-size:9.5px;padding:3px 8px;border-radius:6px}
.dc-addr{font-size:10.5px;color:var(--mt);margin-top:8px;display:flex;align-items:center;gap:4px}
.review-card{background:var(--s1);border:1px solid var(--b1);border-radius:12px;padding:12px;margin-bottom:8px}
.rc-top{display:flex;align-items:center;justify-content:space-between;margin-bottom:8px}
.rc-user{font-size:12px;font-weight:600}
.rc-date{font-size:10px;color:var(--mt)}
.rc-stars{color:var(--gold);font-size:12px;margin-bottom:6px}
.rc-text{font-size:12.5px;color:var(--tx2);line-height:1.6}
.rc-tags{display:flex;flex-wrap:wrap;gap:5px;margin-top:8px}
.rc-tag{background:var(--s2);border:1px solid var(--b1);border-radius:6px;font-size:9.5px;color:var(--mt);padding:2px 8px}
#ov{position:fixed;inset:0;z-index:200;background:rgba(0,0,0,.65);backdrop-filter:blur(6px);display:none;align-items:flex-end}
#ov.on{display:flex}
#sheet{width:100%;max-height:88vh;background:linear-gradient(170deg,#070e2a,#03081a);border:1px solid var(--b2);border-bottom:none;border-radius:24px 24px 0 0;padding:0 0 calc(env(safe-area-inset-bottom)+16px);transform:translateY(100%);transition:transform .35s cubic-bezier(.34,1.56,.64,1);overflow-y:auto;-webkit-overflow-scrolling:touch}
#sheet.on{transform:translateY(0)}
.shandle{width:36px;height:4px;background:var(--b2);border-radius:2px;margin:12px auto 15px}
#sbody{padding:0 18px 8px}
.stit{font-size:17px;font-weight:800;letter-spacing:-.025em;margin-bottom:3px}
.ssub2{font-size:11px;color:var(--mt);font-family:var(--fm);margin-bottom:14px}
.ssec{font-size:9.5px;font-weight:700;color:var(--mt);letter-spacing:.12em;text-transform:uppercase;margin-bottom:7px}
.sp{font-size:13px;color:var(--tx2);line-height:1.7;margin-bottom:14px}
.sscript{background:rgba(248,175,35,.05);border:1px solid rgba(248,175,35,.2);border-left:3px solid var(--gold);border-radius:0 10px 10px 0;padding:11px 13px;font-size:13px;line-height:1.68;color:var(--tx);font-style:italic;margin-bottom:13px}
.slocked{background:linear-gradient(135deg,var(--prd),var(--gg));border:1px solid rgba(168,85,247,.25);border-radius:14px;padding:15px;margin-bottom:13px;text-align:center}
.slocked-i{font-size:24px;margin-bottom:8px}
.slocked-t{font-size:12.5px;color:var(--tx2);line-height:1.6;margin-bottom:11px}
.sunlock{width:100%;background:linear-gradient(135deg,var(--gold),var(--amb));border:none;border-radius:12px;padding:13px;font-family:var(--fn);font-size:14px;font-weight:700;color:#02060e;cursor:pointer;box-shadow:0 4px 18px rgba(248,175,35,.28)}
.sbts{display:flex;gap:8px}
.sbt{flex:1;background:var(--s2);border:1px solid var(--b2);border-radius:12px;padding:12px;font-family:var(--fn);font-size:13px;font-weight:600;color:var(--tx2);cursor:pointer;display:flex;align-items:center;justify-content:center;gap:6px;transition:all .18s}
.sbt.copy{background:var(--gd);border-color:rgba(248,175,35,.28);color:var(--gold)}
.sbt:active{opacity:.8;transform:scale(.97)}
#paymod{position:fixed;inset:0;z-index:300;background:rgba(0,0,0,.8);backdrop-filter:blur(10px);display:none;align-items:flex-end;justify-content:center}
#paymod.on{display:flex}
.paycard{width:100%;max-width:440px;background:linear-gradient(160deg,#070e2a,#030b1a);border:1px solid var(--b2);border-radius:24px 24px 0 0;padding:0 0 calc(env(safe-area-inset-bottom)+8px);max-height:96vh;overflow-y:auto}
.paybody{padding:18px 17px 0}
.pay-hdl{width:36px;height:4px;background:var(--b2);border-radius:2px;margin:12px auto 15px}
.pay-hero{text-align:center;margin-bottom:16px}
.pay-hero-t{font-size:19px;font-weight:800;letter-spacing:-.025em;margin-bottom:4px}
.pay-hero-s{font-size:12px;color:var(--tx2);line-height:1.55}
.plans{display:grid;grid-template-columns:1fr 1fr 1fr;gap:7px;margin-bottom:14px}
.plan{border-radius:13px;padding:12px 8px;text-align:center;cursor:pointer;transition:all .2s;position:relative;background:var(--s1);border:1px solid var(--b1)}
.plan.sel{background:rgba(248,175,35,.09);border-color:rgba(248,175,35,.4)}
.plan-hot{position:absolute;top:-9px;left:50%;transform:translateX(-50%);background:var(--grn);color:#02100a;font-size:8px;font-weight:800;padding:2px 7px;border-radius:10px;white-space:nowrap}
.plan-best{position:absolute;top:-9px;left:50%;transform:translateX(-50%);background:var(--pur);color:#fff;font-size:8px;font-weight:800;padding:2px 7px;border-radius:10px;white-space:nowrap}
.plan-lbl{font-size:9px;font-weight:700;letter-spacing:.08em;color:var(--mt);margin-bottom:5px;text-transform:uppercase}
.plan.sel .plan-lbl{color:var(--gold)}
.plan-price{font-family:var(--fm);font-size:17px;font-weight:800;color:var(--tx2)}
.plan.sel .plan-price{color:var(--gold)}
.plan-note{font-size:9px;color:var(--mt);margin-top:3px}
.cur-row{display:flex;gap:6px;margin-bottom:14px}
.curbt{flex:1;padding:8px;border-radius:10px;border:1px solid var(--b1);background:var(--s1);color:var(--tx2);font-size:12px;font-weight:700;cursor:pointer;font-family:var(--fn);text-align:center;transition:all .18s}
.curbt.on{background:rgba(248,175,35,.1);border-color:rgba(248,175,35,.4);color:var(--gold)}
.vprops{background:rgba(20,207,138,.05);border:1px solid rgba(20,207,138,.15);border-radius:12px;padding:12px 13px;margin-bottom:13px}
.vp-t{font-size:10.5px;color:var(--grn);font-weight:700;margin-bottom:7px}
.vp-l{font-size:11.5px;color:var(--tx2);line-height:1.85}
.pay-div{display:flex;align-items:center;gap:10px;margin-bottom:12px}
.pay-div::before,.pay-div::after{content:'';flex:1;height:1px;background:var(--b1)}
.pay-div-t{font-size:9.5px;color:var(--mt);font-family:var(--fm);white-space:nowrap}
.methods{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:12px}
.mbt{background:var(--s1);border:1px solid var(--b1);border-radius:13px;padding:12px 10px;text-align:center;cursor:pointer;transition:all .2s}
.mbt.sel{border-color:rgba(248,175,35,.4);background:rgba(248,175,35,.06)}
.mbt:active{transform:scale(.97)}
.mico{font-size:22px;margin-bottom:4px}
.mname{font-size:11px;font-weight:600;color:var(--tx2)}
.qrz{display:none;background:var(--s1);border:1px solid var(--b2);border-radius:14px;padding:15px;text-align:center;margin-bottom:12px}
.qrz.on{display:block}
.qrz img{width:100%;max-width:200px;border-radius:10px;margin:0 auto 10px;display:block}
.qinst{font-size:11.5px;color:var(--tx2);line-height:1.65}
.qamt{font-family:var(--fm);font-size:14px;font-weight:700;color:var(--gold);background:rgba(248,175,35,.07);border-radius:8px;padding:8px 12px;margin-top:8px;display:inline-block}
.qaddr{font-family:var(--fm);font-size:10px;color:var(--tx2);word-break:break-all;background:rgba(255,255,255,.04);border:1px solid var(--b1);border-radius:8px;padding:8px 10px;margin:8px 0;text-align:left}
.qcopy{background:var(--gd);border:1px solid rgba(248,175,35,.3);color:var(--gold);border-radius:8px;padding:8px 16px;font-size:12px;font-weight:700;cursor:pointer;font-family:var(--fn);margin-top:6px}
.pay-confirm{width:100%;background:linear-gradient(135deg,var(--gold),var(--amb));border:none;border-radius:13px;padding:14px;font-family:var(--fn);font-size:14px;font-weight:700;color:#020609;cursor:pointer;display:none;margin-bottom:10px;box-shadow:0 4px 22px rgba(248,175,35,.28)}
.pay-cancel{width:100%;background:var(--s1);border:1px solid var(--b1);border-radius:13px;padding:12px;font-family:var(--fn);font-size:13px;color:var(--tx2);cursor:pointer}
#authmod{position:fixed;inset:0;z-index:400;background:rgba(0,0,0,.8);backdrop-filter:blur(10px);display:none;align-items:center;justify-content:center;padding:20px}
#authmod.on{display:flex}
.authcard{width:100%;max-width:360px;background:linear-gradient(160deg,#070e2a,#030b1a);border:1px solid var(--b2);border-radius:22px;padding:26px 20px}
.auth-av{width:60px;height:60px;border-radius:50%;overflow:hidden;margin:0 auto 13px;border:2.5px solid rgba(248,175,35,.5);box-shadow:0 0 20px rgba(248,175,35,.3)}
.auth-av img{width:100%;height:100%;object-fit:cover;object-position:center top}
.auth-t{font-size:17px;font-weight:800;text-align:center;margin-bottom:4px;letter-spacing:-.02em}
.auth-s{font-size:12px;color:var(--tx2);text-align:center;line-height:1.55;margin-bottom:18px}
.auth-btn{width:100%;background:linear-gradient(135deg,var(--gold),var(--amb));border:none;border-radius:12px;padding:14px;font-family:var(--fn);font-size:14px;font-weight:700;color:#020609;cursor:pointer;margin-top:4px;margin-bottom:8px}
.auth-skip{width:100%;background:none;border:none;font-family:var(--fn);font-size:13px;color:var(--mt);cursor:pointer;padding:6px}
.auth-toggle{text-align:center;font-size:12px;color:var(--mt);margin-top:8px}
.auth-toggle span{color:var(--gold);cursor:pointer;font-weight:600}
#umenu{position:fixed;top:calc(var(--hh) + env(safe-area-inset-top) + 6px);right:12px;z-index:150;background:linear-gradient(160deg,#070e2a,#04081e);border:1px solid var(--b2);border-radius:16px;padding:15px;min-width:210px;display:none;box-shadow:0 8px 32px rgba(0,0,0,.5)}
#umenu.on{display:block}
.uav-row{display:flex;align-items:center;gap:10px;margin-bottom:12px;padding-bottom:12px;border-bottom:1px solid var(--b1)}
.uav-img{width:36px;height:36px;border-radius:50%;overflow:hidden;border:2px solid rgba(248,175,35,.4)}
.uav-img img{width:100%;height:100%;object-fit:cover;object-position:center top}
.uname{font-size:13px;font-weight:700}
.uemail{font-size:10px;color:var(--mt)}
.utier{display:inline-flex;align-items:center;gap:4px;background:var(--grd);color:var(--grn);border:1px solid rgba(20,207,138,.25);border-radius:5px;padding:2px 6px;font-size:9px;font-weight:700;margin-top:3px}
.utier.pro{background:var(--gd);color:var(--gold);border-color:rgba(248,175,35,.3)}
.ubt{width:100%;background:var(--s1);border:1px solid var(--b1);border-radius:10px;padding:10px 12px;font-family:var(--fn);font-size:13px;color:var(--tx2);cursor:pointer;text-align:left;display:flex;align-items:center;gap:8px;margin-bottom:6px;transition:all .18s}
.ubt:last-child{margin-bottom:0}
.ubt:active{background:var(--s2)}
.ubt.danger{color:var(--red)}
.ubt svg{width:15px;height:15px;stroke:currentColor;fill:none;stroke-width:2;flex-shrink:0}
#dregmod{position:fixed;inset:0;z-index:350;background:rgba(0,0,0,.8);backdrop-filter:blur(10px);display:none;align-items:flex-end;justify-content:center}
#dregmod.on{display:flex}
.dregcard{width:100%;max-width:440px;background:linear-gradient(160deg,#070e2a,#030b1a);border:1px solid var(--b2);border-radius:24px 24px 0 0;padding:0 0 calc(env(safe-area-inset-bottom)+12px);max-height:95vh;overflow-y:auto}
.dreg-body{padding:0 18px 8px}
.star-input{display:flex;gap:8px;margin-bottom:12px}
.star-input span{font-size:28px;cursor:pointer;opacity:.35;transition:opacity .15s,transform .15s}
.star-input span.on{opacity:1;transform:scale(1.1)}
.toast{position:fixed;bottom:calc(var(--nh) + env(safe-area-inset-bottom) + 16px);left:50%;transform:translateX(-50%) translateY(20px);z-index:500;font-size:13px;font-weight:700;padding:10px 18px;border-radius:22px;opacity:0;transition:all .3s cubic-bezier(.34,1.56,.64,1);white-space:nowrap;pointer-events:none}
.toast.on{opacity:1;transform:translateX(-50%) translateY(0)}
#splash{position:fixed;inset:0;z-index:600;background:var(--bg);display:flex;flex-direction:column;align-items:center;justify-content:center;gap:13px;transition:opacity .55s,transform .55s}
#splash.out{opacity:0;transform:scale(.96);pointer-events:none}
.spl-av{width:86px;height:86px;border-radius:50%;overflow:hidden;border:3px solid rgba(248,175,35,.6);box-shadow:0 0 40px rgba(248,175,35,.4),0 0 80px rgba(248,175,35,.14);animation:spanim 1.4s ease infinite alternate}
.spl-av img{width:100%;height:100%;object-fit:cover;object-position:center top}
@keyframes spanim{from{box-shadow:0 0 30px rgba(248,175,35,.35),0 0 60px rgba(248,175,35,.1)}to{box-shadow:0 0 55px rgba(248,175,35,.6),0 0 110px rgba(248,175,35,.2)}}
.spl-n{font-size:22px;font-weight:800;letter-spacing:-.03em}
.spl-n span{color:var(--gold)}
.spl-s{font-family:var(--fm);font-size:9.5px;color:var(--mt);letter-spacing:.18em;text-align:center}
.spl-tag{background:linear-gradient(135deg,var(--prd),var(--gg));border:1px solid rgba(168,85,247,.3);border-radius:20px;padding:5px 14px;font-size:10px;font-weight:600;color:var(--pur);letter-spacing:.08em}
.spl-bar{width:160px;height:2px;background:var(--s1);border-radius:1px;overflow:hidden}
.spl-fill{height:100%;background:linear-gradient(90deg,var(--gold),var(--pur),var(--grn));animation:sb 2.2s ease forwards}
@keyframes sb{from{width:0%}to{width:100%}}
</style>
</head>
<body>
<div id="splash">
  <div class="spl-av"><img src="LOGO_BASE64_HERE" alt="Sunny"></div>
  <div class="spl-n">Zenith <span>Auto</span> α</div>
  <div class="spl-s">AI-POWERED CAR ARBITRAGE INTELLIGENCE</div>
  <div class="spl-tag">🤖 三核 AI · Qwen + Gemini + Claude 4.6</div>
  <div class="spl-bar"><div class="spl-fill"></div></div>
</div>
<div id="app">
<header id="hdr">
  <div class="logo" onclick="toggleMenu()">
    <div class="logo-av"><img src="LOGO_BASE64_HERE" alt="Sunny"></div>
    <div><div class="logo-n">Zenith <em>Auto</em> α</div><div class="logo-s" id="hdrsub">AI ARBITRAGE INTELLIGENCE</div></div>
  </div>
  <div class="hdr-r">
    <div class="lrow">
      <button class="lp on" onclick="setLang('zh')">中文</button>
      <button class="lp" onclick="setLang('en')">EN</button>
      <button class="lp" onclick="setLang('ja')">日本</button>
      <button class="lp" onclick="setLang('es')">ES</button>
    </div>
    <div class="uav" id="uav" onclick="toggleMenu()"><svg viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 00-4-4H8a4 4 0 00-4 4v2M12 11a4 4 0 100-8 4 4 0 000 8z"/></svg></div>
  </div>
</header>
<div id="ticker"><div class="tt" id="tt"></div></div>
<div id="cnt">
<div id="sc-audit" class="sc">
  <div class="badge">🔍 <span data-k="ab">报价审计</span></div>
  <div class="stitle" data-kh="at">报价单<br><span class="g">极限审计台</span></div>
  <div class="ssub" data-k="as">Gemini Vision真实OCR · 免费看风险 · Pro解锁Claude 4.6律师话术</div>
  <div class="stabs">
    <button class="stab on" onclick="setSubTab('audit','quote',this)">报价审计</button>
    <button class="stab" onclick="setSubTab('audit','contract',this)">合同审查</button>
  </div>
  <div id="audit-quote" class="stab-pane on">
    <input type="file" id="fup" accept="image/*,.pdf" onchange="handleFileUpload(this)">
    <div id="upzone" onclick="document.getElementById('fup').click()">
      <div class="upico"><svg viewBox="0 0 24 24"><path d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"/></svg></div>
      <div class="upt" data-k="ut">点击上传报价单图片</div>
      <div class="ups" data-k="us">JPG · PNG · PDF · Gemini Vision 真实识别</div>
    </div>
    <div class="scanp" id="scanp"><div class="scantr"><div class="scanf" id="scanf"></div></div><div class="scantxt" id="scantxt">INITIALIZING...</div><div class="scanmod" id="scanmod">Gemini 2.0 Flash · OCR</div></div>
    <button class="btn-g" id="demobtn" onclick="runDemoScan()" style="margin-top:10px">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>
      <span data-k="db">⚡ 演示 · 加载示例报价单</span>
    </button>
    <div id="arep"></div>
  </div>
  <div id="audit-contract" class="stab-pane">
    <div class="card" style="margin-bottom:12px">
      <div style="font-size:13.5px;font-weight:700;margin-bottom:6px">📋 合同智能审查</div>
      <div style="font-size:12px;color:var(--tx2);line-height:1.65;margin-bottom:12px">上传购车合同图片，Claude 4.6逐条分析，识别所有对消费者不利的隐藏条款。</div>
      <input type="file" id="fcon" accept="image/*" onchange="handleContractUpload(this)" style="display:none">
      <div onclick="document.getElementById('fcon').click()" style="border:1.5px dashed rgba(248,175,35,.3);border-radius:12px;padding:20px;text-align:center;cursor:pointer;background:rgba(248,175,35,.018)">
        <div style="font-size:24px;margin-bottom:8px">📄</div>
        <div style="font-size:13px;font-weight:600;margin-bottom:3px">点击上传合同图片</div>
        <div style="font-size:11px;color:var(--mt)">支持JPG · PNG · 清晰照片即可</div>
      </div>
    </div>
    <div id="con-result"></div>
    <div class="card">
      <div style="font-size:10px;font-weight:700;color:var(--pur);letter-spacing:.1em;text-transform:uppercase;margin-bottom:6px">PRO 功能</div>
      <div style="font-size:12.5px;color:var(--tx2);line-height:1.7">· 违约金条款风险识别<br>· 交付时间不确定性分析<br>· 隐性强制消费条款检出<br>· 可直接使用的修改建议话术</div>
    </div>
  </div>
</div>
<div id="sc-tco" class="sc">
  <div class="badge">📊 <span data-k="tb">TCO 精算</span></div>
  <div class="stitle" data-kh="tt2">全生命周期<br><span class="g">精算中心</span></div>
  <div class="ssub">Gemini实时精算 · 电车vs油车 · 贷款方案对比</div>
  <div class="stabs">
    <button class="stab on" onclick="setSubTab('tco','tco',this)">TCO对比</button>
    <button class="stab" onclick="setSubTab('tco','loan',this)">贷款精算</button>
  </div>
  <div id="tco-tco" class="stab-pane on">
    <div class="tcocars">
      <div class="ccard"><div class="cclbl a" data-k="ca">车型 A</div>
        <select class="csel" id="sela" onchange="calcTCO()">
          <option value="model3">Tesla Model 3</option><option value="modely">Tesla Model Y</option>
          <option value="bydhan">BYD 汉 EV</option><option value="bydseagull">BYD 海鸥</option>
          <option value="xpengp7">小鹏 P7i</option><option value="nioet7">蔚来 ET7</option>
          <option value="lixiang">理想 L7</option><option value="seres">问界 M7</option>
        </select><div id="badgea"></div></div>
      <div class="ccard"><div class="cclbl b" data-k="cb">车型 B</div>
        <select class="csel" id="selb" onchange="calcTCO()">
          <option value="magotan">VW 迈腾</option><option value="camry">丰田凯美瑞</option>
          <option value="bmw3">BMW 3系</option><option value="accord">本田雅阁</option>
          <option value="mercc">奔驰 C级</option><option value="audi_a4">奥迪 A4L</option>
        </select><div id="badgeb"></div></div>
    </div>
    <div class="tparams fi f1">
      <div class="seclbl" data-k="pl">精算参数</div>
      <div class="prow"><div class="phdr"><span class="plbl" data-k="py">持有年限</span><span class="pval" id="pvyr">5 年</span></div><input type="range" class="pslider" id="slyr" min="1" max="10" value="5" oninput="updParam('years',this.value)"></div>
      <div class="prow"><div class="phdr"><span class="plbl" data-k="pk">年行驶里程</span><span class="pval" id="pvkm">2.0 万km</span></div><input type="range" class="pslider" id="slkm" min="5000" max="60000" step="1000" value="20000" oninput="updParam('km',this.value)"></div>
      <div class="prow"><div class="phdr"><span class="plbl" data-k="pf">油价</span><span class="pval" id="pvfl">¥7.5/L</span></div><input type="range" class="pslider" id="slfl" min="5.0" max="12.0" step="0.1" value="7.5" oninput="updParam('fuel',this.value)"></div>
      <div class="prow"><div class="phdr"><span class="plbl" data-k="pe">电价</span><span class="pval" id="pvel">¥0.60/kWh</span></div><input type="range" class="pslider" id="slel" min="0.3" max="1.5" step="0.05" value="0.6" oninput="updParam('elec',this.value)"></div>
    </div>
    <div class="card fi f2" id="tcard"></div>
    <div class="tverdict fi f3" id="tverd"></div>
  </div>
  <div id="tco-loan" class="stab-pane">
    <div class="card">
      <div class="seclbl">贷款方案</div>
      <div class="prow"><div class="phdr"><span class="plbl">车价</span><span class="pval" id="lv-price">¥250,000</span></div><input type="range" class="pslider" id="sl-lp" min="50000" max="800000" step="5000" value="250000" oninput="calcLoan()"></div>
      <div class="prow"><div class="phdr"><span class="plbl">首付比例</span><span class="pval" id="lv-down">30%</span></div><input type="range" class="pslider" id="sl-ld" min="10" max="70" step="5" value="30" oninput="calcLoan()"></div>
      <div class="prow"><div class="phdr"><span class="plbl">贷款年限</span><span class="pval" id="lv-term">3 年</span></div><input type="range" class="pslider" id="sl-lt" min="1" max="5" value="3" oninput="calcLoan()"></div>
      <div class="prow"><div class="phdr"><span class="plbl">年利率</span><span class="pval" id="lv-rate">4.35%</span></div><input type="range" class="pslider" id="sl-lr" min="2.5" max="12" step="0.1" value="4.35" oninput="calcLoan()"></div>
    </div>
    <div class="loan-result fi f2" id="loan-result"></div>
    <div class="card fi f3" style="margin-top:4px">
      <div class="seclbl">🚨 隐藏费用提醒</div>
      <div style="font-size:12.5px;color:var(--tx2);line-height:1.8">· <strong style="color:var(--red)">金融服务费</strong>：¥3,000–12,000，无法律依据，可拒绝<br>· <strong style="color:var(--amb)">贷款手续费</strong>：正常在贷款金额0.5%以内<br>· <strong style="color:var(--amb)">GPS费捆绑</strong>：常与贷款方案捆绑，可单独拒绝<br>· <strong style="color:var(--gold)">实际APR</strong>通常比名义利率高 <span style="font-family:var(--fm);color:var(--red)">0.5%–3%</span></div>
    </div>
  </div>
</div>
<div id="sc-chat" class="sc">
  <div style="display:flex;align-items:center;gap:8px;margin-bottom:8px;flex-shrink:0">
    <div class="badge" style="margin-bottom:0">🤖 <span data-k="chb">AI 谈判副驾</span></div>
    <div class="tiertag t-free" id="ctier">Qwen FREE</div>
  </div>
  <div class="stabs" style="flex-shrink:0">
    <button class="stab on" onclick="setChatMode('chat',this)">AI 顾问</button>
    <button class="stab" onclick="setChatMode('nego',this)">谈判模拟</button>
  </div>
  <div id="nego-banner" style="display:none" class="nego-banner">
    <div class="nego-av">🎭</div>
    <div>
      <div class="nego-info-t">谈判模拟模式</div>
      <div class="nego-info-s">AI扮演4S销售员 · 练习真实谈判场景</div>
      <button class="nego-btn" onclick="startNego()">▶ 开始模拟谈判</button>
    </div>
  </div>
  <div class="chips" id="chips"></div>
  <div id="chatmsgs"></div>
  <div class="cinput">
    <div class="cinbox">
      <textarea id="ci" rows="1" placeholder="问我任何购车问题…"></textarea>
      <button class="sbtn" onclick="sendMsg()"><svg viewBox="0 0 24 24"><path d="M22 2L11 13M22 2l-7 20-4-9-9-4 20-7z"/></svg></button>
    </div>
  </div>
</div>
<div id="sc-radar" class="sc">
  <div class="badge">📡 <span data-k="rb">价格雷达</span></div>
  <div class="stitle" data-kh="rt">市场行情<br><span class="g">价格雷达</span></div>
  <div class="ssub">AI扫描全网报价，锁定最低落地价与最佳出手时机</div>
  <div class="rsel-wrap fi f1"><span class="rsel-lbl">选择车型：</span>
    <select class="rsel" id="rcar" onchange="renderRadar()">
      <option value="model3">Tesla Model 3</option><option value="modely">Tesla Model Y</option>
      <option value="bydhan">BYD 汉 EV</option><option value="bydseagull">BYD 海鸥</option>
      <option value="lixiang">理想 L7</option><option value="magotan">VW 迈腾</option>
      <option value="camry">丰田凯美瑞</option><option value="bmw3">BMW 3系</option>
      <option value="accord">本田雅阁</option><option value="mercc">奔驰 C级</option>
    </select></div>
  <div id="sigbox" class="fi f2"></div>
  <div class="card fi f3" id="rlist"></div>
  <div class="card fi f4">
    <div class="seclbl">30日价格走势</div>
    <div class="tchart" id="tchart"></div>
    <div style="display:flex;justify-content:space-between;padding:0 3px"><span style="font-size:9.5px;color:var(--mt)">30天前</span><span style="font-size:9.5px;color:var(--mt)">今日</span></div>
  </div>
</div>
<div id="sc-dealers" class="sc">
  <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:9px">
    <div class="badge" style="margin-bottom:0">🏪 经销商社区</div>
    <button onclick="openDealerReg()" style="background:var(--gd);border:1px solid rgba(248,175,35,.3);color:var(--gold);border-radius:10px;padding:6px 12px;font-family:var(--fn);font-size:11px;font-weight:700;cursor:pointer">+ 入驻认证</button>
  </div>
  <div class="stitle">经销商<br><span class="g">评价社区</span></div>
  <div class="ssub">真实成交价 · 用户评分 · 避坑标签 · Zenith认证透明经销商</div>
  <div>
    <div class="dealer-filters" id="brand-filters">
      <div class="df on" onclick="filterDealers('brand','全部',this)">全部品牌</div>
      <div class="df" onclick="filterDealers('brand','Tesla',this)">Tesla</div>
      <div class="df" onclick="filterDealers('brand','BMW',this)">BMW</div>
      <div class="df" onclick="filterDealers('brand','BYD',this)">BYD</div>
      <div class="df" onclick="filterDealers('brand','Toyota',this)">丰田</div>
      <div class="df" onclick="filterDealers('brand','Mercedes',this)">奔驰</div>
      <div class="df" onclick="filterDealers('brand','理想',this)">理想</div>
      <div class="df" onclick="filterDealers('brand','Volkswagen',this)">大众</div>
    </div>
    <div class="dealer-filters" id="city-filters">
      <div class="df on" onclick="filterDealers('city','全部',this)">全部城市</div>
      <div class="df" onclick="filterDealers('city','北京',this)">北京</div>
      <div class="df" onclick="filterDealers('city','上海',this)">上海</div>
      <div class="df" onclick="filterDealers('city','广州',this)">广州</div>
      <div class="df" onclick="filterDealers('city','深圳',this)">深圳</div>
      <div class="df" onclick="filterDealers('city','成都',this)">成都</div>
    </div>
  </div>
  <div id="dealer-list"></div>
</div>
</div>
<nav id="nav">
  <button class="nb on" id="nb-audit" onclick="goTo('audit')"><svg viewBox="0 0 24 24"><path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg><span data-k="na">审计</span></button>
  <button class="nb" id="nb-tco" onclick="goTo('tco')"><svg viewBox="0 0 24 24"><path d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/></svg><span data-k="nt">精算</span></button>
  <button class="nb" id="nb-chat" onclick="goTo('chat')"><svg viewBox="0 0 24 24"><path d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/></svg><span data-k="nc">副驾</span></button>
  <button class="nb" id="nb-radar" onclick="goTo('radar')"><svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="2"/><path d="M16.24 7.76a6 6 0 010 8.49m-8.48-.01a6 6 0 010-8.49m11.31-2.82a10 10 0 010 14.14m-14.14 0a10 10 0 010-14.14"/></svg><span data-k="nr">雷达</span></button>
  <button class="nb" id="nb-dealers" onclick="goTo('dealers')"><svg viewBox="0 0 24 24"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/><polyline points="9,22 9,12 15,12 15,22"/></svg><span>经销商</span></button>
</nav>
</div>
<div id="ov" onclick="closeSheet(event)"><div id="sheet"><div class="shandle"></div><div id="sbody"></div></div></div>
<div id="paymod" onclick="closePay(event)">
  <div class="paycard" id="paycard"><div class="pay-hdl"></div><div class="paybody">
    <div class="pay-hero"><div style="font-size:28px;margin-bottom:8px">🔓</div><div class="pay-hero-t">解锁 Claude 4.6 专家版</div><div class="pay-hero-s">律师级谈判话术 · 合同逐条审查 · 真实AI分析</div></div>
    <div class="plans">
      <div class="plan sel" id="pl-single" onclick="selPlan('single')"><div class="plan-lbl">单次</div><div class="plan-price" id="pp-single">¥29.9</div><div class="plan-note">1次审计</div></div>
      <div class="plan" id="pl-monthly" onclick="selPlan('monthly')"><div class="plan-hot">热门</div><div class="plan-lbl">月度</div><div class="plan-price" id="pp-monthly">¥99</div><div class="plan-note">无限次/月</div></div>
      <div class="plan" id="pl-yearly" onclick="selPlan('yearly')"><div class="plan-best">最划算</div><div class="plan-lbl">年度</div><div class="plan-price" id="pp-yearly">¥599</div><div class="plan-note">省¥589</div></div>
    </div>
    <div class="cur-row"><button class="curbt on" id="cur-cny" onclick="setCur('cny')">🇨🇳 人民币</button><button class="curbt" id="cur-usd" onclick="setCur('usd')">🇺🇸 USD</button></div>
    <div class="vprops"><div class="vp-t">✅ 解锁后获得</div><div class="vp-l">· Claude 4.6 律师级谈判话术（可直接说）<br>· 合同逐条法律风险分析<br>· 每项费用法律依据 + 成功率预测<br>· 平均帮用户节省 <strong style="color:var(--gold)">¥8,000–25,000</strong></div></div>
    <div class="pay-div"><div class="pay-div-t">选择支付方式</div></div>
    <div class="methods">
      <div class="mbt" onclick="selMethod('wechat')"><div class="mico">💚</div><div class="mname">微信支付</div></div>
      <div class="mbt" onclick="selMethod('alipay')"><div class="mico">💙</div><div class="mname">支付宝</div></div>
      <div class="mbt" onclick="selMethod('usdt')"><div class="mico">🟢</div><div class="mname">USDT</div></div>
      <div class="mbt" onclick="selMethod('paypal')"><div class="mico">🔵</div><div class="mname">PayPal</div></div>
    </div>
    <div class="qrz" id="qz-wechat"><img src="WECHAT_BASE64_HERE" alt="WeChat Pay"><div class="qinst">微信扫码付款</div><div class="qamt" id="qa-wechat">¥29.9</div><div class="qinst" style="margin-top:6px;font-size:11px;color:var(--mt)">备注：Zenith · 付款后点击解锁</div></div>
    <div class="qrz" id="qz-alipay"><img src="ALIPAY_BASE64_HERE" alt="Alipay"><div class="qinst">支付宝扫码付款</div><div class="qamt" id="qa-alipay">¥29.9</div></div>
    <div class="qrz" id="qz-usdt"><div style="font-size:30px;margin-bottom:10px">💎</div><div style="font-size:12px;font-weight:700;color:var(--gold);margin-bottom:8px">USDT · TRC20</div><div class="qaddr">TL4FQUjtgz7gy6UMEx5gZsZkK2Npmkq1YH</div><div class="qamt" id="qa-usdt">$3.99 USDT</div><button class="qcopy" onclick="copyTxt('TL4FQUjtgz7gy6UMEx5gZsZkK2Npmkq1YH')">📋 复制地址</button></div>
    <div class="qrz" id="qz-paypal"><div style="font-size:30px;margin-bottom:10px">🅿️</div><a id="ppal-link" href="https://www.paypal.me/Sunnyday168/3.99" target="_blank" style="display:block;background:linear-gradient(135deg,#003087,#009cde);color:#fff;padding:13px 20px;border-radius:11px;font-weight:700;font-size:14px;text-decoration:none;margin-bottom:8px">PayPal · 付 $3.99</a><div class="qinst" style="color:var(--mt);font-size:11px">备注 Zenith · 付款后点击解锁</div></div>
    <button class="pay-confirm" id="payconfirm" onclick="confirmPay()">✅ 我已付款 · 立即解锁专家版</button>
    <button class="pay-cancel" onclick="closePay()">暂不升级</button>
    <div style="height:8px"></div>
  </div></div>
</div>
<div id="authmod"><div class="authcard">
  <div class="auth-av"><img src="LOGO_BASE64_HERE" alt="Sunny"></div>
  <div class="auth-t" id="auth-title">创建免费账户</div>
  <div class="auth-s">保存分析记录，跨设备同步Pro会员</div>
  <input type="text" id="auth-name" placeholder="昵称（可选）" autocomplete="name" style="margin-bottom:10px">
  <input type="email" id="auth-email" placeholder="邮箱地址" autocomplete="email" style="margin-bottom:10px">
  <input type="password" id="auth-pass" placeholder="密码（6位以上）" autocomplete="new-password" style="margin-bottom:10px">
  <button class="auth-btn" onclick="doAuth()">🚀 创建账户 · 免费使用</button>
  <button class="auth-skip" onclick="closeAuth()">跳过</button>
  <div class="auth-toggle">已有账户？<span onclick="toggleAuthMode()">点击登录</span></div>
</div></div>
<div id="umenu">
  <div class="uav-row"><div class="uav-img"><img src="LOGO_BASE64_HERE" alt="Sunny"></div><div><div class="uname" id="um-name">游客</div><div class="uemail" id="um-email">未登录</div><div class="utier" id="um-tier">FREE</div></div></div>
  <button class="ubt" id="um-login" onclick="showAuth();toggleMenu()"><svg viewBox="0 0 24 24"><path d="M15 3h4a2 2 0 012 2v14a2 2 0 01-2 2h-4M10 17l5-5-5-5M15 12H3"/></svg>登录 / 注册</button>
  <button class="ubt" id="um-upgrade" onclick="openPay();toggleMenu()" style="display:none"><svg viewBox="0 0 24 24"><path d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>升级 Pro · Claude 4.6</button>
  <button class="ubt" id="um-dealer" onclick="openDealerReg();toggleMenu()"><svg viewBox="0 0 24 24"><path d="M3 9l9-7 9 7v11a2 2 0 01-2 2H5a2 2 0 01-2-2z"/></svg>经销商入驻申请</button>
  <button class="ubt danger" id="um-logout" onclick="doLogout();toggleMenu()" style="display:none"><svg viewBox="0 0 24 24"><path d="M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4M16 17l5-5-5-5M21 12H9"/></svg>退出登录</button>
</div>
<div id="dregmod" onclick="closeDealerReg(event)"><div class="dregcard"><div class="shandle" style="margin:12px auto 14px"></div><div class="dreg-body">
  <div style="font-size:18px;font-weight:800;margin-bottom:4px">🏪 经销商入驻申请</div>
  <div style="font-size:12px;color:var(--tx2);margin-bottom:16px">通过Zenith认证，获得金标认证徽章</div>
  <div class="seclbl">基本信息</div>
  <input type="text" id="dreg-name" placeholder="经销商名称" style="margin-bottom:9px">
  <input type="text" id="dreg-brand" placeholder="主营品牌（如：BMW / Tesla）" style="margin-bottom:9px">
  <input type="text" id="dreg-city" placeholder="城市" style="margin-bottom:9px">
  <input type="text" id="dreg-addr" placeholder="详细地址" style="margin-bottom:9px">
  <input type="tel" id="dreg-phone" placeholder="联系电话" style="margin-bottom:16px">
  <div class="seclbl">认证承诺 <span style="color:var(--grn);font-size:9px">·全部必选</span></div>
  <div style="display:flex;flex-direction:column;gap:8px;margin-bottom:16px">
    <label style="display:flex;align-items:center;gap:10px;font-size:12.5px;color:var(--tx2);cursor:pointer"><input type="checkbox" id="dreg-c1" style="width:16px;height:16px;accent-color:var(--gold);flex-shrink:0">承诺不强制收取金融服务费</label>
    <label style="display:flex;align-items:center;gap:10px;font-size:12.5px;color:var(--tx2);cursor:pointer"><input type="checkbox" id="dreg-c2" style="width:16px;height:16px;accent-color:var(--gold);flex-shrink:0">承诺不强制贴膜/GPS等附加项目</label>
    <label style="display:flex;align-items:center;gap:10px;font-size:12.5px;color:var(--tx2);cursor:pointer"><input type="checkbox" id="dreg-c3" style="width:16px;height:16px;accent-color:var(--gold);flex-shrink:0">同意公示真实成交价格范围</label>
    <label style="display:flex;align-items:center;gap:10px;font-size:12.5px;color:var(--tx2);cursor:pointer"><input type="checkbox" id="dreg-c4" style="width:16px;height:16px;accent-color:var(--gold);flex-shrink:0">违规将被撤销认证并公示</label>
  </div>
  <button class="btn-g" onclick="submitDealerReg()" style="margin-top:0">提交入驻申请</button>
  <div style="font-size:11px;color:var(--mt);text-align:center;margin-top:10px;line-height:1.6">认证费：¥1,650/月 · 审核3-5工作日</div>
  <div style="height:8px"></div>
</div></div></div>
<div id="revmod" style="position:fixed;inset:0;z-index:350;display:none;align-items:flex-end;justify-content:center;background:rgba(0,0,0,.8);backdrop-filter:blur(8px)">
  <div id="revcard" style="width:100%;max-width:440px;background:linear-gradient(160deg,#070e2a,#030b1a);border:1px solid var(--b2);border-radius:24px 24px 0 0;padding:0 0 calc(env(safe-area-inset-bottom)+12px);max-height:90vh;overflow-y:auto">
    <div class="shandle" style="margin:12px auto 14px"></div>
    <div style="padding:0 18px 8px">
      <div style="font-size:17px;font-weight:800;margin-bottom:4px" id="rev-dealer-name">写评价</div>
      <div style="font-size:11px;color:var(--mt);margin-bottom:14px">帮助其他车主避坑</div>
      <div class="seclbl">评分</div>
      <div class="star-input" id="star-input"><span onclick="setStars(1)">⭐</span><span onclick="setStars(2)">⭐</span><span onclick="setStars(3)">⭐</span><span onclick="setStars(4)">⭐</span><span onclick="setStars(5)">⭐</span></div>
      <div class="seclbl">遇到的问题（可多选）</div>
      <div style="display:flex;flex-wrap:wrap;gap:7px;margin-bottom:14px" id="avoid-tag-select">
        <div class="df" onclick="toggleAvoidTag(this,'金融服务费')">金融服务费</div>
        <div class="df" onclick="toggleAvoidTag(this,'强制贴膜')">强制贴膜</div>
        <div class="df" onclick="toggleAvoidTag(this,'出库费')">出库费</div>
        <div class="df" onclick="toggleAvoidTag(this,'GPS强制')">GPS强制</div>
        <div class="df" onclick="toggleAvoidTag(this,'强制延保')">强制延保</div>
        <div class="df" onclick="toggleAvoidTag(this,'价格虚高')">价格虚高</div>
        <div class="df" onclick="toggleAvoidTag(this,'服务态度差')">服务态度差</div>
        <div class="df" onclick="toggleAvoidTag(this,'无问题')">无问题 ✓</div>
      </div>
      <div class="seclbl">详细评价</div>
      <textarea id="rev-text" rows="4" placeholder="描述你的购车经历，帮助其他车主做决策…" style="margin-bottom:14px;resize:none"></textarea>
      <button class="btn-g" onclick="submitReview()" style="margin-top:0">提交评价</button>
      <div style="height:6px"></div>
    </div>
  </div>
</div>
<div class="toast" id="toast"></div>
<script>
window.onerror=function(m,s,l,c,e){console.warn('ZA:',m,'L'+l);return true;};
window.onunhandledrejection=function(e){console.warn('ZA promise:',e.reason);e.preventDefault();};
const PLANS={single:{cny:29.9,usd:3.99},monthly:{cny:99,usd:13.99},yearly:{cny:599,usd:79.9}};
const CLERK_KEY='pk_test_c3RlcmxpbmctcGlwZWZpc2gtOS5jbGVyay5hY2NvdW50cy5kZXYk';
let lang='zh',curSc='audit',chatLog=[],isPro=false,curUser=null;
let tcp={years:5,km:20000,fuel:7.5,elec:0.6};
let selPlanKey='single',selCur='cny',selMethodKey=null;
let authMode='register',chatMode='chat',negoActive=false;
let curDealerId=null,revStars=0,revAvoidTags=[];
let filterBrand='全部',filterCity='全部';
let scanDone=false,clerkInst=null;
function lsGet(k){try{const v=localStorage.getItem(k);if(!v)return null;try{return JSON.parse(v);}catch{return v;}}catch{return null;}}
function lsSet(k,v){try{localStorage.setItem(k,typeof v==='object'?JSON.stringify(v):String(v));}catch{}}
function lsDel(k){try{localStorage.removeItem(k);}catch{}}
async function initClerk(){
  let tries=0;while(!window.Clerk&&tries<25){await new Promise(r=>setTimeout(r,200));tries++;}
  if(!window.Clerk)return;
  try{const clerk=new window.Clerk(CLERK_KEY);await clerk.load();clerkInst=clerk;if(clerk.user)setUserFromClerk(clerk.user);clerk.addListener(({user})=>{if(user)setUserFromClerk(user);else clearClerkUser();});}catch(e){console.warn('Clerk:',e.message);}
}
function setUserFromClerk(cu){const name=cu.firstName||cu.username||cu.emailAddresses?.[0]?.emailAddress?.split('@')[0]||'用户';const email=cu.emailAddresses?.[0]?.emailAddress||'';curUser={id:cu.id,name,email,fromClerk:true};isPro=lsGet('pro_'+cu.id)==='1';updateUserUI();}
function clearClerkUser(){curUser=null;isPro=lsGet('pro_guest')==='1';updateUserUI();}
function doAuth(){const email=document.getElementById('auth-email').value.trim();const pass=document.getElementById('auth-pass').value;const name=document.getElementById('auth-name').value.trim()||email.split('@')[0];if(!email){showToast('请填写邮箱','red');return;}if(pass.length<6){showToast('密码至少6位','red');return;}const id='u_'+btoa(email).replace(/[=+/]/g,'').substring(0,16);curUser={id,name,email};lsSet('zuser',curUser);isPro=lsGet('pro_'+id)==='1';updateUserUI();closeAuth();showToast('👋 欢迎，'+name+'！','green');}
function doLogout(){if(clerkInst&&curUser?.fromClerk){clerkInst.signOut().catch(()=>{});return;}curUser=null;lsDel('zuser');isPro=lsGet('pro_guest')==='1';updateUserUI();showToast('已退出登录');}
function toggleAuthMode(){authMode=authMode==='register'?'login':'register';const isL=authMode==='login';document.getElementById('auth-title').textContent=isL?'登录账户':'创建免费账户';document.querySelector('.auth-btn').textContent=isL?'🔐 登录':'🚀 创建账户 · 免费使用';document.querySelector('.auth-toggle').innerHTML=isL?'没有账户？<span onclick="toggleAuthMode()">立即注册</span>':'已有账户？<span onclick="toggleAuthMode()">点击登录</span>';document.getElementById('auth-name').style.display=isL?'none':'block';}
function updateUserUI(){const n=curUser?.name||'游客';const e=curUser?.email||'未登录';setEl('um-name',n);setEl('um-email',e);document.getElementById('um-login')?.style.setProperty('display',curUser?'none':'flex');document.getElementById('um-logout')?.style.setProperty('display',curUser?'flex':'none');document.getElementById('um-upgrade')?.style.setProperty('display',isPro?'none':'flex');if(curUser){const av=document.getElementById('uav');if(av)av.innerHTML='<div style="width:100%;height:100%;background:linear-gradient(135deg,var(--gold),var(--amb));display:flex;align-items:center;justify-content:center;font-size:13px;font-weight:800;color:#02060e">'+n[0].toUpperCase()+'</div>';}if(isPro)updateProUI();}
function updateProUI(){const tier=document.getElementById('um-tier');if(tier){tier.textContent='⭐ PRO';tier.className='utier pro';}const ctier=document.getElementById('ctier');if(ctier){ctier.textContent='Claude 4.6 EXPERT';ctier.className='tiertag t-pro';}const sub=document.getElementById('hdrsub');if(sub)sub.textContent='⭐ PRO · Claude 4.6 已解锁';document.getElementById('um-upgrade')?.style.setProperty('display','none');}
function showAuth(){document.getElementById('authmod').classList.add('on');}
function closeAuth(){document.getElementById('authmod').classList.remove('on');}
function toggleMenu(){document.getElementById('umenu').classList.toggle('on');}
document.addEventListener('click',e=>{const m=document.getElementById('umenu');if(!m)return;const uav=document.getElementById('uav');const logo=document.querySelector('.logo');if(!m.contains(e.target)&&(!uav||!uav.contains(e.target))&&(!logo||!logo.contains(e.target)))m.classList.remove('on');});
function openPay(){document.getElementById('paymod').classList.add('on');setTimeout(()=>{selPlan('single');setCur('cny');},30);}
function closePay(e){if(e&&e.target!==document.getElementById('paymod'))return;document.getElementById('paymod').classList.remove('on');resetPayUI();}
function resetPayUI(){document.querySelectorAll('.qrz').forEach(z=>z.classList.remove('on'));document.querySelectorAll('.mbt').forEach(b=>b.classList.remove('sel'));const c=document.getElementById('payconfirm');if(c)c.style.display='none';selMethodKey=null;}
function selPlan(k){selPlanKey=k;['single','monthly','yearly'].forEach(p=>document.getElementById('pl-'+p)?.classList.toggle('sel',p===k));updatePrices();}
function setCur(c){selCur=c;document.getElementById('cur-cny')?.classList.toggle('on',c==='cny');document.getElementById('cur-usd')?.classList.toggle('on',c==='usd');updatePrices();}
function updatePrices(){const p=PLANS[selPlanKey]||PLANS.single;const isCNY=selCur==='cny';['single','monthly','yearly'].forEach(k=>{const el=document.getElementById('pp-'+k);if(el)el.textContent=isCNY?('¥'+PLANS[k].cny):('$'+PLANS[k].usd);});const qa_w=document.getElementById('qa-wechat');if(qa_w)qa_w.textContent='¥'+p.cny;const qa_a=document.getElementById('qa-alipay');if(qa_a)qa_a.textContent='¥'+p.cny;const qa_u=document.getElementById('qa-usdt');if(qa_u)qa_u.textContent='$'+p.usd+' USDT';const pp=document.getElementById('ppal-link');if(pp){pp.href='https://www.paypal.me/Sunnyday168/'+p.usd;pp.textContent='PayPal · 付 $'+p.usd;}}
function selMethod(m){selMethodKey=m;document.querySelectorAll('.mbt').forEach((b,i)=>b.classList.toggle('sel',['wechat','alipay','usdt','paypal'][i]===m));document.querySelectorAll('.qrz').forEach(z=>z.classList.remove('on'));document.getElementById('qz-'+m)?.classList.add('on');const c=document.getElementById('payconfirm');if(c)c.style.display='block';}
function confirmPay(){isPro=true;const key=curUser?('pro_'+curUser.id):'pro_guest';lsSet(key,'1');document.getElementById('paymod').classList.remove('on');resetPayUI();updateProUI();initChat();if(scanDone)renderAudit(window._lastAuditData||null);showToast('🎉 Pro解锁！Claude 4.6全力以赴','gold');}
function initTicker(){const D=[['TSLA','¥249,900','↑2.1%','up'],['BYD-HAN','¥189,800','↓1.3%','dn'],['BMW3','¥329,900','↑0.5%','up'],['CAMRY','¥183,800','→','nt'],['VW-MGT','¥175,900','↓0.8%','dn'],['理想L7','¥319,800','↑1.2%','up'],['问界M7','¥289,800','↓0.4%','dn'],['蔚来ET7','¥358,000','→','nt'],['A4L','¥319,800','↓1.5%','dn'],['BYD-鸥','¥79,800','↑3.2%','up'],['奔驰C','¥359,800','↑0.7%','up'],['雅阁','¥179,800','↓0.3%','dn']];const h=[...D,...D].map(([c,p,ch,d])=>'<span class="ti">'+c+' <strong style="color:var(--gold)">'+p+'</strong> <em class="'+d+'">'+ch+'</em></span>').join('');const tt=document.getElementById('tt');if(tt)tt.innerHTML=h+h;}
const SCS=['audit','tco','chat','radar','dealers'];
let txS=0,tyS=0;
function goTo(name){if(curSc===name)return;const o=document.getElementById('sc-'+curSc);if(o){o.classList.remove('vis');setTimeout(()=>o.classList.remove('on'),260);}document.querySelectorAll('.nb').forEach(b=>b.classList.remove('on'));document.getElementById('nb-'+name)?.classList.add('on');curSc=name;const el=document.getElementById('sc-'+name);if(el){el.classList.add('on');requestAnimationFrame(()=>requestAnimationFrame(()=>el.classList.add('vis')));}if(name==='tco')calcTCO();if(name==='radar')renderRadar();if(name==='dealers')renderDealers();if(navigator.vibrate)navigator.vibrate(8);}
document.addEventListener('touchstart',e=>{txS=e.changedTouches[0].screenX;tyS=e.changedTouches[0].screenY;},{passive:true});
document.addEventListener('touchend',e=>{const dx=e.changedTouches[0].screenX-txS,dy=e.changedTouches[0].screenY-tyS;if(Math.abs(dx)<70||Math.abs(dy)>Math.abs(dx)*.75)return;const i=SCS.indexOf(curSc);if(dx<0&&i<SCS.length-1)goTo(SCS[i+1]);if(dx>0&&i>0)goTo(SCS[i-1]);},{passive:true});
function setSubTab(screen,tab,btn){document.querySelectorAll('#sc-'+screen+' .stab').forEach(b=>b.classList.remove('on'));document.querySelectorAll('#sc-'+screen+' .stab-pane').forEach(p=>p.classList.remove('on'));if(btn)btn.classList.add('on');const pane=document.getElementById(screen+'-'+tab);if(pane)pane.classList.add('on');}
function setChatMode(mode,btn){chatMode=mode;document.querySelectorAll('#sc-chat .stab').forEach(b=>b.classList.remove('on'));if(btn)btn.classList.add('on');const nb=document.getElementById('nego-banner');if(nb)nb.style.display=mode==='nego'?'flex':'none';if(mode==='chat'){negoActive=false;initChat();}}
function setEl(id,v){const e=document.getElementById(id);if(e)e.textContent=v;}
function esc(t){return String(t).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');}
function fmt(n){return '¥'+Math.round(n).toLocaleString();}
function copyTxt(txt){if(navigator.clipboard){navigator.clipboard.writeText(txt).then(()=>showToast('✓ 已复制','green'));}else{const ta=document.createElement('textarea');ta.value=txt;document.body.appendChild(ta);ta.select();document.execCommand('copy');document.body.removeChild(ta);showToast('✓ 已复制','green');}}
function showToast(msg,type){const el=document.getElementById('toast');if(!el)return;el.textContent=msg;el.style.background=type==='gold'?'var(--gold)':type==='red'?'var(--red)':'var(--grn)';el.style.color=type==='red'?'#fff':'#020609';el.classList.add('on');setTimeout(()=>el.classList.remove('on'),2400);}
function countUp(id,target,pfx,dur){pfx=pfx||'¥';dur=dur||900;const el=document.getElementById(id);if(!el)return;const s=Date.now();const tick=()=>{const p=Math.min((Date.now()-s)/dur,1),ea=1-Math.pow(1-p,3);el.textContent=pfx+Math.round(target*ea).toLocaleString();if(p<1)requestAnimationFrame(tick);};requestAnimationFrame(tick);}
function renderMd(text){let h=esc(text).replace(/\*\*(.*?)\*\*/g,'<strong>$1</strong>').replace(/`(.*?)`/g,'<span class="mk">$1</span>').replace(/^> (.+)$/gm,'<blockquote>$1</blockquote>');const lines=h.split('\n');let out='',inT=false,th='';for(const ln of lines){if(ln.trim().startsWith('|')&&ln.trim().endsWith('|')){if(!inT){inT=true;th='<table>';}const cs=ln.split('|').filter((_,j,a)=>j>0&&j<a.length-1).map(c=>c.trim());if(!cs.every(c=>/^[-:]+$/.test(c))){const tag=th==='<table>'?'th':'td';th+='<tr>'+cs.map(c=>'<'+tag+'>'+c+'</'+tag+'>').join('')+'</tr>';}}else{if(inT){out+=th+'</table>';inT=false;th='';}out+=ln+'\n';}}if(inT)out+=th+'</table>';return out.replace(/---+/g,'<hr style="border:none;border-top:1px solid var(--b1);margin:7px 0">').replace(/\n\n/g,'<br><br>').replace(/\n/g,' ').replace(/ +/g,' ');}
function mdLight(t){return String(t).replace(/\*\*(.*?)\*\*/g,'<strong>$1</strong>');}
const SCAN_STEPS=['初始化 Gemini Vision OCR…','上传图片至AI服务器…','识别报价格式与字段…','比对全国价格数据库…','生成Zenith专家报告…','✅ 真实AI分析完成'];
const SCAN_MODS=['Gemini 2.0 Flash · Upload','Gemini 2.0 Flash · OCR','Gemini · Field Extract','Qwen · DB Reference','Claude 4.6 · Expert'];
let scanTimer=null;
const DEMO_DATA={carModel:'Tesla Model 3 后驱版',dealerName:'某4S经销商',totalPrice:284380,items:[{name:'金融服务费',amount:8800,category:'danger',reason:'不贷款时零法律依据，违规强收'},{name:'出库检测费',amount:2500,category:'danger',reason:'PDI为经销商义务，不得转嫁消费者'},{name:'强制贴膜套餐',amount:4800,category:'danger',reason:'非法规要求，强制收费属违规'},{name:'车载GPS定位器',amount:1800,category:'danger',reason:'批发成本¥200-400，溢价350%+'},{name:'延保套餐(3年)',amount:5500,category:'warning',reason:'有价值但偏高，市场价¥2000-3000'},{name:'上牌代办费',amount:1200,category:'warning',reason:'市场价¥500-700，此价偏高'},{name:'裸车价',amount:249900,category:'ok',reason:'合规'},{name:'购置税(10%)',amount:22718,category:'ok',reason:'合规'},{name:'交强险(首年)',amount:950,category:'ok',reason:'合规'},{name:'商业险(基础版)',amount:5200,category:'ok',reason:'合规'},{name:'车船税',amount:480,category:'ok',reason:'合规'},{name:'牌照工本费',amount:150,category:'ok',reason:'合规'}]};
const SCRIPTS_ZH={'金融服务费':'我选择全款提车，根据《汽车销售管理办法》第十条，金融服务费在不使用金融服务时不得强制收取。请立即从报价单删除，否则我向12315平台投诉。','出库检测费':'PDI出库检测是贵店对厂家的义务，相关成本已包含在经销商保证金中。请提供此项收费的合同依据，否则我要求删除，并向市场监管局投诉。','强制贴膜套餐':'贴膜不是法规要求，我不需要此服务。强制收取¥4800涉嫌违规，请删除。如不删除我将在汽车之家和小红书发布本报价单。','车载GPS定位器':'同规格GPS电商零售价不超过¥400，此项加价350%+不合理，且存在数据隐私风险。请以成本价提供或删除。','延保套餐(3年)':'延保有兴趣，但¥5500超出市场价位。同等第三方延保约¥2000-3000，请调整到¥2800以内。','上牌代办费':'上牌代办市场价¥500-700，¥1200偏高。请调整到¥700以内，否则我自行办理。'};
function handleFileUpload(input){if(!input.files||!input.files[0])return;const file=input.files[0];const reader=new FileReader();reader.onload=function(e){const base64=e.target.result.split(',')[1];const mimeType=file.type||'image/jpeg';runRealScan(base64,mimeType,'quote');};reader.readAsDataURL(file);}
function handleContractUpload(input){if(!input.files||!input.files[0])return;const file=input.files[0];const reader=new FileReader();reader.onload=function(e){runContractScan(e.target.result.split(',')[1],file.type||'image/jpeg');};reader.readAsDataURL(file);}
async function runRealScan(b64,mime,type){if(type==='demo'||!b64){runDemoAnim();return;}const zone=document.getElementById('upzone');const sp=document.getElementById('scanp');const sf=document.getElementById('scanf');const st=document.getElementById('scantxt');const sm=document.getElementById('scanmod');if(zone)zone.classList.add('scanning');if(sp)sp.classList.add('on');document.getElementById('demobtn').style.display='none';let pct=0,step=0;clearInterval(scanTimer);scanTimer=setInterval(()=>{pct+=1.4+Math.random()*.8;if(pct>=90)pct=90;if(sf)sf.style.width=pct+'%';const si=Math.floor(pct/90*(SCAN_STEPS.length-2));if(si!==step){step=si;if(st)st.textContent=SCAN_STEPS[si];if(sm)sm.textContent=SCAN_MODS[Math.min(si,SCAN_MODS.length-1)];}},80);try{const resp=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({image:b64,mimeType:mime,type:type||'quote'})});const data=await resp.json();clearInterval(scanTimer);if(sf)sf.style.width='100%';if(st)st.textContent=SCAN_STEPS[SCAN_STEPS.length-1];setTimeout(()=>{if(zone)zone.classList.remove('scanning');if(sp)sp.classList.remove('on');scanDone=true;renderAudit(data.success&&data.data&&data.data.items?data.data:DEMO_DATA);},600);}catch(err){clearInterval(scanTimer);if(zone)zone.classList.remove('scanning');if(sp)sp.classList.remove('on');scanDone=true;renderAudit(DEMO_DATA);showToast('网络异常，已加载演示数据');}}
function runDemoScan(){document.getElementById('demobtn').style.display='none';runDemoAnim();}
function runDemoAnim(){const zone=document.getElementById('upzone');const sp=document.getElementById('scanp');const sf=document.getElementById('scanf');const st=document.getElementById('scantxt');const sm=document.getElementById('scanmod');if(zone)zone.classList.add('scanning');if(sp)sp.classList.add('on');let pct=0,step=0;clearInterval(scanTimer);scanTimer=setInterval(()=>{pct+=1.6+Math.random()*.8;if(pct>=100)pct=100;if(sf)sf.style.width=pct+'%';const si=Math.floor(pct/100*(SCAN_STEPS.length-1));if(si!==step){step=si;if(st)st.textContent=SCAN_STEPS[si];if(sm)sm.textContent=SCAN_MODS[Math.min(si,SCAN_MODS.length-1)];}if(pct>=100){clearInterval(scanTimer);if(zone)zone.classList.remove('scanning');if(sp)sp.classList.remove('on');scanDone=true;renderAudit(DEMO_DATA);}},55);}
async function runContractScan(b64,mime){const cr=document.getElementById('con-result');if(cr)cr.innerHTML='<div style="padding:16px;text-align:center"><span class="spin"></span><span style="color:var(--pur);font-size:13px">Claude 4.6 正在审查合同…</span></div>';try{const resp=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({image:b64,mimeType:mime,type:'contract'})});const data=await resp.json();if(data.success&&data.data)renderContractResult(data.data);else if(cr)cr.innerHTML='<div class="card" style="color:var(--amb)">无法解析合同内容，请确保图片清晰</div>';}catch(e){if(cr)cr.innerHTML='<div class="card" style="color:var(--red)">网络错误，请重试</div>';}}
function renderContractResult(data){const cr=document.getElementById('con-result');if(!cr)return;if(!data.issues||!data.issues.length){cr.innerHTML='<div class="card"><div style="color:var(--grn);font-weight:700;margin-bottom:6px">✅ 未发现明显风险条款</div><div style="font-size:12.5px;color:var(--tx2)">合同基本合规，仍建议仔细阅读后再签署。</div></div>';return;}const risk=data.riskLevel==='high'?'var(--red)':data.riskLevel==='medium'?'var(--amb)':'var(--gold)';let h='<div class="savebanner" style="margin-bottom:11px"><div><div class="savelbl">合同风险等级</div><div style="font-size:22px;font-weight:800;color:'+risk+'">'+ {high:'⚠️ 高风险',medium:'🟡 中等风险',low:'🟢 低风险'}[data.riskLevel||'low']+'</div></div><div style="text-align:right"><div style="font-size:20px;font-weight:700;color:'+risk+'">'+data.issues.length+'项</div><div style="font-size:9px;color:var(--mt)">风险条款</div></div></div>';data.issues.forEach((issue,i)=>{const cls=issue.severity==='danger'?'danger':'warning';h+='<div class="arow '+cls+' fi f'+Math.min(i+2,5)+'" onclick="openContractIssue(\''+encodeURIComponent(JSON.stringify(issue))+'\')">'+'<div class="dot '+cls+'"></div><div class="rname">'+esc(issue.clause)+'</div>'+'<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="rgba(255,255,255,.3)" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg></div>';});if(data.summary)h+='<div class="card" style="margin-top:8px"><div style="font-size:10px;color:var(--mt);margin-bottom:5px;font-weight:700;letter-spacing:.1em">AI总结</div><div style="font-size:13px;color:var(--tx2);line-height:1.7">'+esc(data.summary)+'</div></div>';cr.innerHTML=h;}
function openContractIssue(encoded){let issue;try{issue=JSON.parse(decodeURIComponent(encoded));}catch{return;}document.getElementById('sbody').innerHTML='<div class="stit">'+esc(issue.clause)+'</div><div class="ssub2">'+(issue.severity==='danger'?'🔴 HIGH RISK':'🟡 WARNING')+'</div><div class="ssec">风险分析</div><div class="sp">'+esc(issue.risk)+'</div>'+(issue.script?'<div class="ssec">建议话术</div><div class="sscript" id="sct">'+esc(issue.script)+'</div>':'')+'<div class="sbts">'+(issue.script?'<button class="sbt copy" onclick="copyTxt(document.getElementById(\'sct\').textContent)">📋 复制话术</button>':'')+'<button class="sbt" onclick="closeSheet()">关闭</button></div>';openSheet();}
window._lastAuditData=null;
function renderAudit(data){if(!data)data=DEMO_DATA;window._lastAuditData=data;const items=data.items||[];const danger=items.filter(r=>r.category==='danger');const warn=items.filter(r=>r.category==='warning');const ok=items.filter(r=>r.category==='ok');const save=danger.reduce((s,r)=>s+(r.amount||0),0);let h='<div class="savebanner fi f1"><div><div class="savelbl">AI识别可节省金额</div><div class="saveamt" id="savc">¥0</div></div><div style="text-align:right"><div style="font-size:9px;color:var(--mt);margin-bottom:2px">识别风险项</div><div style="font-family:var(--fm);font-size:22px;font-weight:800;color:var(--red)">'+(danger.length+warn.length)+'项</div></div></div>';if(data.carModel)h+='<div style="font-size:12px;color:var(--mt);margin-bottom:12px;font-family:var(--fm)">🚗 '+esc(data.carModel)+(data.dealerName?' · '+esc(data.dealerName):'')+'</div>';h+='<div class="astats fi f2"><div class="astat"><div class="asv" style="color:var(--red)">'+danger.length+'</div><div class="asl">高风险</div></div><div class="astat"><div class="asv" style="color:var(--amb)">'+warn.length+'</div><div class="asl">可议价</div></div><div class="astat"><div class="asv" style="color:var(--grn)">'+ok.length+'</div><div class="asl">合规</div></div></div>';h+='<div class="seclbl fi f3">高风险项 — '+(isPro?'Claude 4.6分析':'免费查看 · 话术需Pro')+'</div>';danger.forEach((r,i)=>{h+='<div class="arow danger fi f'+Math.min(i+4,5)+'" onclick="openAuditItem(\''+encodeURIComponent(JSON.stringify(r))+'\')">'+'<div class="dot danger"></div><div class="rname">'+esc(r.name)+'<span class="aitag">Gemini OCR</span></div>'+'<div class="ramt">-'+fmt(r.amount)+'</div><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="rgba(255,255,255,.3)" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg></div>';});if(warn.length){h+='<div class="seclbl fi f4" style="margin-top:4px">可议价项</div>';warn.forEach((r,i)=>{h+='<div class="arow warning fi f'+Math.min(i+4,5)+'" onclick="openAuditItem(\''+encodeURIComponent(JSON.stringify(r))+'\')">'+'<div class="dot warning"></div><div class="rname">'+esc(r.name)+'</div><div class="ramt">-'+fmt(r.amount)+'</div><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="rgba(255,255,255,.3)" stroke-width="2"><path d="M9 18l6-6-6-6"/></svg></div>';});}if(ok.length){h+='<div class="seclbl fi f5" style="margin-top:4px">合规费用</div>';ok.forEach(r=>{h+='<div class="arow ok"><div class="dot ok"></div><div class="rname">'+esc(r.name)+'</div><div class="ramt">'+fmt(r.amount)+'</div></div>';});}const rep=document.getElementById('arep');if(rep){rep.innerHTML=h;rep.style.display='block';}countUp('savc',save,'¥');}
async function openAuditItem(encoded){let r;try{r=JSON.parse(decodeURIComponent(encoded));}catch{return;}document.getElementById('sbody').innerHTML='<div class="stit">'+esc(r.name)+'</div><div class="ssub2">'+(r.category==='danger'?'🔴 HIGH RISK':'🟡 WARNING')+' · '+fmt(r.amount)+'</div><div class="ssec">AI 分析 <span class="aitag">'+(isPro?'Claude 4.6':'Qwen Free')+'</span></div><div class="sp" id="ai-a"><span class="spin"></span>'+(isPro?'Claude 4.6 生成律师级话术…':'Qwen 分析中…')+'</div><div id="ai-s"></div><div class="sbts"><button class="sbt" onclick="closeSheet()">关闭</button></div>';openSheet();if(isPro){try{const resp=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({tier:'pro',lang,messages:[{role:'user',content:'购车报价中"'+r.name+'"收费'+fmt(r.amount)+'。请给出：①60字分析；②律师级谈判话术（引号包裹，可直接说）；③成功率预测。'}]})});const d=await resp.json();const txt=d.content||r.reason||'分析完成';document.getElementById('ai-a').textContent=txt;document.getElementById('ai-s').innerHTML='<div class="ssec">谈判话术 <span class="aitag">Claude 4.6</span></div><div class="sscript" id="sct">'+esc(txt)+'</div><div class="sbts"><button class="sbt copy" onclick="copyTxt(document.getElementById(\'sct\').textContent)">📋 复制话术</button><button class="sbt" onclick="closeSheet()">关闭</button></div>';}catch(e){const script=SCRIPTS_ZH[r.name]||'请依据法规与经销商协商，要求删除此项不合理收费。';document.getElementById('ai-a').textContent=r.reason||'此项费用存在违规风险';document.getElementById('ai-s').innerHTML='<div class="ssec">话术</div><div class="sscript" id="sct">'+esc(script)+'</div><div class="sbts"><button class="sbt copy" onclick="copyTxt(document.getElementById(\'sct\').textContent)">📋 复制</button><button class="sbt" onclick="closeSheet()">关闭</button></div>';}}else{let analysis='';try{const resp=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({tier:'free',lang,messages:[{role:'user',content:'购车报价"'+r.name+'"收'+fmt(r.amount)+'合理吗？用30字回答。'}]})});const d=await resp.json();analysis=d.content||r.reason||'此项费用存在争议';}catch{analysis=r.reason||'此项费用存在争议';}document.getElementById('ai-a').textContent=analysis;document.getElementById('ai-s').innerHTML='<div class="slocked"><div class="slocked-i">🔒</div><div class="slocked-t">律师级谈判话术由 <strong style="color:var(--gold)">Claude 4.6</strong> 专属生成<br>升级Pro解锁 · 平均节省¥8,000-25,000</div><button class="sunlock" onclick="closeSheet();openPay()">⚡ 解锁Pro · ¥29.9 / $3.99</button></div><div class="sbts"><button class="sbt" onclick="closeSheet()">关闭</button></div>';}}
function openSheet(){const o=document.getElementById('ov');const s=document.getElementById('sheet');o.style.display='flex';o.style.opacity='0';requestAnimationFrame(()=>{o.style.transition='opacity .22s';o.style.opacity='1';s.classList.add('on');});}
function closeSheet(e){if(e&&e.target!==document.getElementById('ov'))return;const o=document.getElementById('ov');const s=document.getElementById('sheet');s.classList.remove('on');o.style.opacity='0';setTimeout(()=>{o.style.display='none';},350);}
const CARS={model3:{n:'Tesla Model 3',t:'EV',p:249900,e:13.5,i:28500,m:7500,d:.35},modely:{n:'Tesla Model Y',t:'EV',p:263900,e:15.0,i:30000,m:8000,d:.36},bydhan:{n:'BYD 汉 EV',t:'EV',p:189800,e:14.8,i:23500,m:9000,d:.40},bydseagull:{n:'BYD 海鸥',t:'EV',p:79800,e:10.5,i:13500,m:5500,d:.38},xpengp7:{n:'小鹏 P7i',t:'EV',p:229900,e:14.0,i:26500,m:8500,d:.42},nioet7:{n:'蔚来 ET7',t:'EV',p:358000,e:14.5,i:42000,m:12000,d:.40},lixiang:{n:'理想 L7',t:'HYBRID',p:319800,e:6.5,i:37500,m:11000,d:.30},seres:{n:'问界 M7',t:'HYBRID',p:289800,e:7.0,i:34500,m:10000,d:.32},magotan:{n:'VW 迈腾',t:'GAS',p:175900,e:7.5,i:23500,m:26000,d:.40},camry:{n:'丰田凯美瑞',t:'GAS',p:183800,e:7.2,i:24500,m:21000,d:.35},bmw3:{n:'BMW 3系',t:'GAS',p:329900,e:7.8,i:46000,m:37000,d:.40},accord:{n:'本田雅阁',t:'GAS',p:179800,e:6.8,i:23500,m:20000,d:.35},mercc:{n:'奔驰 C级',t:'GAS',p:359800,e:8.0,i:49000,m:41000,d:.40},audi_a4:{n:'奥迪 A4L',t:'GAS',p:319800,e:7.6,i:44000,m:35000,d:.42}};
function calcCar(k){const c=CARS[k];const {years,km,fuel,elec}=tcp;const tk=years*km;const en=c.t==='EV'?(c.e/100)*tk*elec:c.t==='HYBRID'?(c.e/100)*tk*(fuel*.45+elec*.12):(c.e/100)*tk*fuel;const ins=c.i*(years/5),mai=c.m*(years/5)*(km/20000),dep=c.p*c.d*Math.min(years/5,1.2);return{p:c.p,en,ins,mai,dep,tot:c.p+en+ins+mai+dep};}
function updParam(k,v){v=parseFloat(v);tcp[k]=v;if(k==='years')setEl('pvyr',v+' 年');if(k==='km')setEl('pvkm',(v/10000).toFixed(1)+' 万km');if(k==='fuel')setEl('pvfl','¥'+v.toFixed(1)+'/L');if(k==='elec')setEl('pvel','¥'+v.toFixed(2)+'/kWh');calcTCO();}
let tcoTimer=null;
function calcTCO(){clearTimeout(tcoTimer);_drawTCO();tcoTimer=setTimeout(fetchTCOAI,1400);}
function _drawTCO(){const ka=document.getElementById('sela')?.value||'model3';const kb=document.getElementById('selb')?.value||'magotan';const a=CARS[ka],b=CARS[kb],ra=calcCar(ka),rb=calcCar(kb);const TL={EV:{zh:'纯电动',en:'Electric',ja:'EV',es:'Eléctrico'},GAS:{zh:'燃油车',en:'Gasoline',ja:'ガソリン',es:'Gasolina'},HYBRID:{zh:'增程/混动',en:'Hybrid',ja:'ハイブリッド',es:'Híbrido'}};const TC={EV:'bev',GAS:'bgas',HYBRID:'bhyb'};const lb=t=>'<span class="tbadge '+TC[t]+'">'+(TL[t]||TL.GAS)[lang]+'</span>';const ba=document.getElementById('badgea');const bb=document.getElementById('badgeb');if(ba)ba.innerHTML=lb(a.t);if(bb)bb.innerHTML=lb(b.t);const mn={zh:['落地价','能源费','保险','保养维修','折旧'],en:['Base','Energy','Insurance','Maintenance','Depreciation'],ja:['車両価格','エネルギー','保険','維持費','減価償却'],es:['Precio','Combustible','Seguro','Mantenimiento','Depreciación']};const rows=[{va:ra.p,vb:rb.p},{va:ra.en,vb:rb.en},{va:ra.ins,vb:rb.ins},{va:ra.mai,vb:rb.mai},{va:ra.dep,vb:rb.dep}];let h='<div class="tcotots"><div class="tcot"><div class="tcotl">'+esc(a.n)+'</div><div class="tcotv a" id="tvA">¥0</div></div><div class="tcot"><div class="tcotl">'+esc(b.n)+'</div><div class="tcotv b" id="tvB">¥0</div></div></div><div class="divider"></div>';(mn[lang]||mn.zh).forEach((name,i)=>{const r=rows[i];const mx=Math.max(r.va,r.vb)||1;h+='<div class="tmet"><div class="tmhdr"><div class="tmn">'+name+'</div><div class="tmvs"><span class="va">'+fmt(r.va)+'</span><span style="color:var(--b2)"> | </span><span class="vb">'+fmt(r.vb)+'</span></div></div><div class="tbars"><div class="tbarbg"><div class="tbarf a" data-w="'+(r.va/mx*100).toFixed(1)+'" style="width:0%"></div></div><div class="tbarbg"><div class="tbarf b" data-w="'+(r.vb/mx*100).toFixed(1)+'" style="width:0%"></div></div></div></div>';});const tc=document.getElementById('tcard');if(tc)tc.innerHTML=h;setTimeout(()=>{document.querySelectorAll('.tbarf').forEach(e=>e.style.width=e.dataset.w+'%');countUp('tvA',ra.tot,'¥');countUp('tvB',rb.tot,'¥');},80);const win=ra.tot<=rb.tot?a:b,diff=fmt(Math.abs(ra.tot-rb.tot));const tv=document.getElementById('tverd');if(tv)tv.innerHTML='<div class="tvlbl">精算结论 <span class="aitag">Gemini精算中</span></div><div class="tvtxt"><strong>'+esc(win.n)+'</strong> 全周期节省 <span class="save">'+diff+'</span></div>';}
async function fetchTCOAI(){const ka=document.getElementById('sela')?.value||'model3';const kb=document.getElementById('selb')?.value||'magotan';const a=CARS[ka],b=CARS[kb],ra=calcCar(ka),rb=calcCar(kb);const tv=document.getElementById('tverd');if(tv)tv.innerHTML='<div class="tvlbl">AI精算结论 <span class="aitag">Gemini</span></div><div class="tvtxt"><span class="spin"></span>Gemini精算中…</div>';try{const resp=await fetch('/api/tco',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({carA:a.n,carB:b.n,tcoA:ra.tot,tcoB:rb.tot,params:tcp,lang})});const d=await resp.json();if(tv)tv.innerHTML='<div class="tvlbl">AI精算结论 <span class="aitag">Gemini 2.0 Flash</span></div><div class="tvtxt">'+(d.verdict?esc(d.verdict):'<strong>'+(ra.tot<=rb.tot?esc(a.n):esc(b.n))+'</strong> 全周期更省 <span class="save">'+fmt(Math.abs(ra.tot-rb.tot))+'</span>')+'</div>';}catch{if(tv)tv.innerHTML='<div class="tvlbl">精算结论</div><div class="tvtxt"><strong>'+(ra.tot<=rb.tot?esc(a.n):esc(b.n))+'</strong> 全周期更省 <span class="save">'+fmt(Math.abs(ra.tot-rb.tot))+'</span></div>';}}
function calcLoan(){const price=parseInt(document.getElementById('sl-lp')?.value||250000);const downPct=parseInt(document.getElementById('sl-ld')?.value||30)/100;const term=parseInt(document.getElementById('sl-lt')?.value||3);const rate=parseFloat(document.getElementById('sl-lr')?.value||4.35)/100;setEl('lv-price','¥'+price.toLocaleString());setEl('lv-down',(downPct*100).toFixed(0)+'%');setEl('lv-term',term+' 年');setEl('lv-rate',(rate*100).toFixed(2)+'%');const down=price*downPct,loan=price-down,months=term*12,mr=rate/12;const monthly=mr===0?loan/months:loan*mr*Math.pow(1+mr,months)/(Math.pow(1+mr,months)-1);const total=monthly*months,interest=total-loan;const lr=document.getElementById('loan-result');if(!lr)return;lr.innerHTML='<div class="seclbl">贷款方案详情</div><div class="loan-grid"><div><div class="loan-item-lbl">首付金额</div><div class="loan-item-val">'+fmt(down)+'</div></div><div><div class="loan-item-lbl">贷款金额</div><div class="loan-item-val">'+fmt(loan)+'</div></div><div><div class="loan-item-lbl">月供</div><div class="loan-item-val">'+fmt(monthly)+'/月</div></div><div><div class="loan-item-lbl">利息总额</div><div class="loan-item-val warn">'+fmt(interest)+'</div></div><div><div class="loan-item-lbl">还款总额</div><div class="loan-item-val warn">'+fmt(total)+'</div></div><div><div class="loan-item-lbl">利息/车价</div><div class="loan-item-val">'+(interest/price*100).toFixed(1)+'%</div></div></div><div style="margin-top:12px;padding-top:12px;border-top:1px solid var(--b1);font-size:12px;color:var(--tx2)">🔑 <strong style="color:var(--gold)">谈判建议：</strong>以月供金额谈。告诉销售月供在<span style="font-family:var(--fm);color:var(--grn)">'+fmt(monthly)+'</span>以内才考虑签单。</div>';}
async function initChat(){chatLog=[];const welcome=isPro?'**Zenith Auto α · Claude 4.6 专家版**\n\n律师级谈判策略 + 合同法律分析。\n\n> 告诉我你的车型、问题或报价单情况。':negoActive?'**[谈判模拟模式]** 我是张经理，负责这款车的销售。您好！\n\n> 请问您今天是看哪款车？':'**Zenith Auto α 在线**\n\n购车精算师，掌握全国底价 + 4S套路解析。\n\n> 免费：基础咨询。**Pro (¥29.9)** 解锁律师话术。';const tier=negoActive?'nego':isPro?'pro':'free';addAI(welcome,tier);const chipSets={zh:['金融服务费怎么砍？','Model 3底价多少？','电车vs油车谁省？','延保值不值？','何时下单最划算？'],en:["Kill finance fee?","Model 3 floor price?","EV vs ICE real cost?","Extended warranty?","Best time to buy?"]};const c=chipSets[lang]||chipSets.zh;const ch=document.getElementById('chips');if(ch)ch.innerHTML=c.map(x=>'<button class="chip" onclick="useChip(this,\''+x.replace(/'/g,"\\'")+'\')"  >'+x+'</button>').join('');}
function useChip(el,t){el.style.background='var(--gd)';el.style.color='var(--gold)';const ci=document.getElementById('ci');if(ci)ci.value=t;sendMsg();}
function startNego(){negoActive=true;chatLog=[];initChat();}
async function sendMsg(){const inp=document.getElementById('ci');const t=inp?.value.trim();if(!t)return;if(inp)inp.value='';if(inp)inp.style.height='auto';addUser(t);const tid=showDots();try{const tier=negoActive?'nego':isPro?'pro':'free';const hist=chatLog.slice(-8).map(m=>({role:m.r==='u'?'user':'assistant',content:m.t}));hist.push({role:'user',content:t});const resp=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({tier,lang,messages:hist})});const d=await resp.json();removeDots(tid);let txt=d.content||'网络异常，请重试';if(!isPro&&!negoActive&&(t.includes('话术')||t.includes('怎么说')))txt+='\n\n---\n💡 *升级Pro (¥29.9) 获取律师话术*';addAI(txt,tier);}catch(e){removeDots(tid);addAI('网络异常，请重试','free');}}
function addUser(t){chatLog.push({r:'u',t});renderChat();}
function addAI(t,tier){chatLog.push({r:'ai',t,tier:tier||'free'});renderChat();scrollChat();}
function renderChat(){const cm=document.getElementById('chatmsgs');if(!cm)return;cm.innerHTML=chatLog.map(m=>{if(m.r==='u')return '<div class="msg u"><div class="bub">'+esc(m.t)+'</div></div>';const tt=m.tier==='pro'?'<div class="tiertag t-pro">Claude 4.6</div>':m.tier==='nego'?'<div class="tiertag t-nego">🎭 谈判模式</div>':'<div class="tiertag t-free">Qwen Free</div>';return '<div class="msg ai"><div class="chav"><img src="LOGO_BASE64_HERE" alt="S"></div><div style="max-width:84%">'+tt+'<div class="bub">'+renderMd(m.t)+'</div></div></div>';}).join('');}
let dc=0;
function showDots(){const id='dt-'+(++dc);const el=document.createElement('div');el.className='msg ai';el.id=id;el.innerHTML='<div class="chav"><img src="LOGO_BASE64_HERE" alt="S"></div><div class="bub"><div class="dots"><div class="dot2"></div><div class="dot2"></div><div class="dot2"></div></div></div>';const cm=document.getElementById('chatmsgs');if(cm)cm.appendChild(el);scrollChat();return id;}
function removeDots(id){document.getElementById(id)?.remove();}
function scrollChat(){setTimeout(()=>{const m=document.getElementById('chatmsgs');if(m)m.scrollTop=m.scrollHeight;},40);}
const RADAR={model3:{s:'nego',txt:{zh:'**当前行情**：库存适中，华北折让最大。目标落地价 **¥241,000–243,000**，月底谈判最佳。',en:'Moderate inventory. North China best. Target ¥241K–243K. Month-end optimal.'},dl:[{n:'特斯拉直营',p:249900,d:0,t:'official'},{n:'华北优选经销商',p:243000,d:-6900,t:'dealer',best:true},{n:'华东经销商',p:245500,d:-4400,t:'dealer'},{n:'华南经销商',p:244800,d:-5100,t:'dealer'},{n:'二手·2.3万km',p:198000,km:2.3,t:'used'}],tr:[249900,249500,248800,249200,248500,247900,248100,247500,246800,247200,246500,246000,245800,245500,245900,245200,244800,245000,244500,244000,244200,243800,243500,243900,244000,243500,243000,242800,243000,242500]},bmw3:{s:'buy',txt:{zh:'**买入信号 🟢**：季末库存压力，华南优惠破历史记录。目标 **¥316,000–320,000**，本周行动。',en:'Buy Signal: Quarter-end pressure. Target ¥316K–320K. Act this week.'},dl:[{n:'BMW官方授权',p:329900,d:0,t:'official'},{n:'华南优选店',p:319800,d:-10100,t:'dealer',best:true},{n:'华东宝马店',p:322000,d:-7900,t:'dealer'},{n:'准新车·0.6万km',p:265000,km:0.6,t:'used'}],tr:[339900,338500,337000,336500,335000,333800,334200,332900,332000,331500,330800,330000,329900,329500,329000,328500,329200,328000,327500,327000,326800,326500,326000,325800,326200,325500,325000,324800,324500,324200]},camry:{s:'wait',txt:{zh:'**观望信号 🟡**：30天涨约2.9%，库存偏紧。等下月补货，预计优惠 **¥4,000–6,000**。',en:'Wait Signal: +2.9% in 30 days. Wait for resupply.'},dl:[{n:'丰田官方授权',p:183800,d:0,t:'official'},{n:'华南优选店',p:181000,d:-2800,t:'dealer',best:true},{n:'华东经销商',p:181500,d:-2300,t:'dealer'},{n:'二手2023款',p:152000,km:2.1,t:'used'}],tr:[183800,183800,184000,184500,184800,183900,183500,183800,184200,184600,185000,185200,185500,185800,186000,186200,186500,186800,187000,187300,187500,187800,188000,188200,188400,188600,188800,189000,189200,189500]}};
function getRadarData(k){if(RADAR[k])return RADAR[k];const carPrices={model3:249900,modely:263900,bydhan:189800,bydseagull:79800,xpengp7:229900,nioet7:358000,lixiang:319800,seres:289800,magotan:175900,mercc:359800,accord:179800};const bp=carPrices[k]||200000;return{s:'nego',txt:{zh:'目前库存正常，有议价空间。目标价 **¥'+Math.round(bp*.96).toLocaleString()+'**，月底签约最佳。',en:'Moderate inventory. Target ¥'+Math.round(bp*.96).toLocaleString()+'. Sign at month end.'},dl:[{n:'官方/授权渠道',p:bp,d:0,t:'official'},{n:'优选经销商',p:Math.round(bp*.96),d:Math.round(-bp*.04),t:'dealer',best:true},{n:'经销商B',p:Math.round(bp*.975),d:Math.round(-bp*.025),t:'dealer'},{n:'二手平台',p:Math.round(bp*.78),km:2.5,t:'used'}],tr:Array.from({length:30},(_,i)=>Math.round(bp*(1-.01*(1-i/30)+.003*Math.sin(i))))};}
function renderRadar(){const k=document.getElementById('rcar')?.value||'model3';const r=getRadarData(k);const ICONS={buy:'🟢',wait:'🟡',nego:'⚡'},TAGS={buy:'立即出手·BUY NOW',wait:'等待时机·WAIT',nego:'强力议价·NEGOTIATE'};const txt=(r.txt&&r.txt[lang])||r.txt?.zh||'';const sb=document.getElementById('sigbox');if(sb)sb.innerHTML='<div class="sig '+r.s+'"><div class="sigico">'+ICONS[r.s]+'</div><div><div class="sigtag">'+TAGS[r.s]+'</div><div class="sigtxt">'+mdLight(txt)+'</div></div></div>';let dlh='<div class="seclbl">经销商比价</div>';r.dl.forEach((d,i)=>{const ps=d.t==='used'?'color:var(--blu)':d.best?'color:var(--grn)':'';let disc='';if(d.d&&d.d!==0)disc='<div class="ddisc '+(d.d<0?'p':'n')+'">'+(d.d<0?'省':'+')+'&nbsp;'+fmt(Math.abs(d.d))+'</div>';else if(d.t==='official')disc='<div class="ddisc">官方价</div>';const meta=d.t==='used'?'二手·'+d.km+'万km':d.t==='official'?'官方渠道':'授权经销商';dlh+='<div class="drow '+(d.best?'best':'')+'"><div class="drank">'+(i+1)+'</div><div class="dinfo"><div class="dname">'+esc(d.n)+'</div><div class="dmeta">'+meta+'</div></div><div class="dprice"><div class="dpv" style="'+ps+'">'+fmt(d.p)+'</div>'+disc+'</div>'+(d.best?'<div class="airec">AI推荐</div>':'')+'</div>';});const rl=document.getElementById('rlist');if(rl)rl.innerHTML=dlh;const tr=r.tr||[];if(tr.length){const mx=Math.max(...tr),mn=Math.min(...tr),rng=mx-mn||1;const tc=document.getElementById('tchart');if(tc)tc.innerHTML=tr.map((p,i)=>{const h=Math.round(((p-mn)/rng)*80+10);const cls=p===mx?'hi':p===mn?'lo':i===tr.length-1?'cur':'';return '<div class="tbar-w"><div class="tbar '+cls+'" style="height:'+h+'%"></div></div>';}).join('');}}
const DEALERS_DB=[{id:'d001',name:'特斯拉体验中心(朝阳)',brand:'Tesla',city:'北京',district:'朝阳区',addr:'朝阳区建国路SOHO T3',phone:'400-920-0608',certified:true,rating:4.6,reviews:142,avgDiscount:3500,msrp:249900,lowestSeen:244000,tags:['官方直营','价格透明','0中间费'],avoidTags:[],since:'2019',type:'official'},{id:'d002',name:'北京宝马优选授权店',brand:'BMW',city:'北京',district:'海淀区',addr:'海淀区中关村大街88号',phone:'010-8881-0000',certified:true,rating:4.1,reviews:89,avgDiscount:12000,msrp:329900,lowestSeen:316000,tags:['折让力度大','季末冲量'],avoidTags:['金融服务费','强制贴膜'],since:'2016',type:'dealer'},{id:'d003',name:'上海丰田凯美瑞旗舰店',brand:'Toyota',city:'上海',district:'浦东新区',addr:'浦东新区张江高科技园区',phone:'021-5888-0000',certified:false,rating:3.8,reviews:56,avgDiscount:2500,msrp:183800,lowestSeen:180000,tags:['库存充足'],avoidTags:['出库检测费¥2800','GPS定位器¥1800'],since:'2018',type:'dealer'},{id:'d004',name:'广州BYD汉EV旗舰店',brand:'BYD',city:'广州',district:'天河区',addr:'天河区天河路385号',phone:'020-3888-0000',certified:true,rating:4.4,reviews:203,avgDiscount:5000,msrp:189800,lowestSeen:183000,tags:['服务好','交付快','价格透明','无隐藏费用'],avoidTags:[],since:'2021',type:'dealer'},{id:'d005',name:'深圳理想汽车体验中心',brand:'理想',city:'深圳',district:'南山区',addr:'南山区科技园南区',phone:'0755-8888-0000',certified:true,rating:4.7,reviews:178,avgDiscount:0,msrp:319800,lowestSeen:319800,tags:['官方直营','无议价','体验好'],avoidTags:[],since:'2022',type:'official'},{id:'d006',name:'成都奔驰授权经销商',brand:'Mercedes',city:'成都',district:'武侯区',addr:'武侯区人民南路4段',phone:'028-8888-0000',certified:false,rating:3.5,reviews:67,avgDiscount:15000,msrp:359800,lowestSeen:342000,tags:['季末优惠大'],avoidTags:['金融服务费¥9800','强制延保¥8800','出库费¥3500'],since:'2015',type:'dealer'},{id:'d007',name:'杭州大众迈腾4S店',brand:'Volkswagen',city:'杭州',district:'滨江区',addr:'滨江区滨和路588号',phone:'0571-8888-0000',certified:true,rating:4.2,reviews:91,avgDiscount:8000,msrp:175900,lowestSeen:167000,tags:['折让到位','售后好'],avoidTags:['上牌代办费¥1500'],since:'2014',type:'dealer'},{id:'d008',name:'武汉本田雅阁专营店',brand:'Honda',city:'武汉',district:'江汉区',addr:'江汉区解放大道1399号',phone:'027-8888-0000',certified:false,rating:3.9,reviews:44,avgDiscount:3000,msrp:179800,lowestSeen:175000,tags:['日系品质','省油'],avoidTags:['精品套餐¥5800强制'],since:'2017',type:'dealer'}];
function getReviews(id){return lsGet('reviews_'+id)||[];}
function saveReview(id,rev){const rs=getReviews(id);rs.unshift(rev);if(rs.length>50)rs.length=50;lsSet('reviews_'+id,rs);}
function renderDealers(){const list=document.getElementById('dealer-list');if(!list)return;const filtered=DEALERS_DB.filter(d=>{if(filterBrand!=='全部'&&d.brand!==filterBrand)return false;if(filterCity!=='全部'&&d.city!==filterCity)return false;return true;});if(!filtered.length){list.innerHTML='<div style="text-align:center;padding:30px;color:var(--mt);font-size:13px">该筛选条件下暂无经销商</div>';return;}const BEM={Tesla:'⚡',BMW:'🔵',Toyota:'🚗',BYD:'🌿',理想:'💎',Mercedes:'⭐',Volkswagen:'🔷',Honda:'🍀'};list.innerHTML=filtered.map(d=>{const reviews=getReviews(d.id);const avgR=reviews.length?((reviews.reduce((s,r)=>s+(r.stars||3),0)/reviews.length)).toFixed(1):d.rating;const revCount=d.reviews+reviews.length;const stars='⭐'.repeat(Math.round(parseFloat(avgR)));const em=BEM[d.brand]||'🏪';const okTags=d.tags.map(t=>'<span class="tag-ok">✓ '+t+'</span>').join('');const badTags=d.avoidTags.map(t=>'<span class="tag-bad">⚠ '+t+'</span>').join('');return '<div class="dealer-card'+(d.certified?' certified':'\"')+' onclick="openDealerDetail(\''+d.id+'\')">'+'<div class="dc-top"><div class="dc-logo">'+em+'</div><div class="dc-info"><div class="dc-name">'+esc(d.name)+(d.certified?'<span class="dc-cert">Zenith认证</span>':'')+'</div><div class="dc-brand">'+esc(d.brand)+' · '+esc(d.city)+'</div><div class="dc-rating"><span class="stars">'+stars+'</span><span style="font-family:var(--fm);font-size:12px;font-weight:700;color:var(--gold)">'+avgR+'</span><span class="dc-rv">('+revCount+'条)</span></div></div></div>'+'<div class="dc-stats"><div class="dc-stat"><div class="dc-stat-v" style="color:var(--gold)">'+fmt(d.avgDiscount)+'</div><div class="dc-stat-l">平均优惠</div></div><div class="dc-stat"><div class="dc-stat-v" style="color:var(--grn)">'+fmt(d.lowestSeen)+'</div><div class="dc-stat-l">历史最低</div></div><div class="dc-stat"><div class="dc-stat-v" style="color:var(--blu)">'+d.since+'</div><div class="dc-stat-l">开业年份</div></div></div>'+(okTags||badTags?'<div class="dc-tags">'+okTags+badTags+'</div>':'')+'<div class="dc-addr">📍 '+esc(d.addr)+'</div></div>';}).join('');}
function filterDealers(type,value,btn){if(type==='brand'){filterBrand=value;document.querySelectorAll('#brand-filters .df').forEach(b=>b.classList.remove('on'));}else{filterCity=value;document.querySelectorAll('#city-filters .df').forEach(b=>b.classList.remove('on'));}if(btn)btn.classList.add('on');renderDealers();}
function openDealerDetail(id){const d=DEALERS_DB.find(x=>x.id===id);if(!d)return;curDealerId=id;const BEM={Tesla:'⚡',BMW:'🔵',Toyota:'🚗',BYD:'🌿',理想:'💎',Mercedes:'⭐',Volkswagen:'🔷',Honda:'🍀'};const em=BEM[d.brand]||'🏪';const reviews=getReviews(id);const avgR=reviews.length?((reviews.reduce((s,r)=>s+(r.stars||3),0)/reviews.length)).toFixed(1):d.rating;const stars='⭐'.repeat(Math.round(parseFloat(avgR)));const allRevs=[...reviews,...(d.avoidTags.length?[{user:'Zenith用户',stars:3,text:'注意：该经销商历史上存在'+d.avoidTags[0]+'等问题。',tags:d.avoidTags.slice(0,2),date:'2025-12'}]:[])];const revHtml=allRevs.length?allRevs.slice(0,5).map(r=>'<div class="review-card"><div class="rc-top"><span class="rc-user">'+esc(r.user||'匿名')+'</span><span class="rc-date">'+esc(r.date||'最近')+'</span></div><div class="rc-stars">'+'⭐'.repeat(r.stars||3)+'</div><div class="rc-text">'+esc(r.text||'')+'</div>'+(r.tags&&r.tags.length?'<div class="rc-tags">'+r.tags.map(t=>'<span class="rc-tag">'+esc(t)+'</span>').join('')+'</div>':'')+'</div>').join(''):'<div style="text-align:center;padding:20px;color:var(--mt);font-size:12.5px">还没有用户评价<br>成为第一个写评价的人</div>';document.getElementById('sbody').innerHTML='<div style="display:flex;align-items:center;gap:10px;margin-bottom:12px"><div style="width:46px;height:46px;border-radius:13px;background:var(--s2);display:flex;align-items:center;justify-content:center;font-size:22px">'+em+'</div><div><div class="stit" style="font-size:15px">'+esc(d.name)+'</div>'+(d.certified?'<span class="dc-cert">Zenith认证</span>':'')+'</div></div>'+'<div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:7px;margin-bottom:14px"><div class="dc-stat"><div class="dc-stat-v" style="color:var(--gold)">'+stars+' '+avgR+'</div><div class="dc-stat-l">综合评分</div></div><div class="dc-stat"><div class="dc-stat-v" style="color:var(--grn)">'+fmt(d.avgDiscount)+'</div><div class="dc-stat-l">平均优惠</div></div><div class="dc-stat"><div class="dc-stat-v" style="color:var(--blu)">'+fmt(d.lowestSeen)+'</div><div class="dc-stat-l">历史最低</div></div></div>'+(d.tags.length?'<div class="seclbl">优点</div><div class="dc-tags" style="margin-bottom:12px">'+d.tags.map(t=>'<span class="tag-ok">✓ '+t+'</span>').join('')+'</div>':'')+(d.avoidTags.length?'<div class="seclbl">避坑提醒</div><div class="dc-tags" style="margin-bottom:14px">'+d.avoidTags.map(t=>'<span class="tag-bad">⚠ '+t+'</span>').join('')+'</div>':'')+'<div class="dc-addr" style="margin-bottom:14px">📍 '+esc(d.addr)+'</div><div style="display:flex;gap:8px;margin-bottom:16px"><button class="btn-g" style="margin-top:0" onclick="openWriteReview(\''+id+'\',\''+esc(d.name)+'\')">✍️ 写评价</button><button class="btn-o" onclick="copyTxt(\''+esc(d.phone)+'\')">📞 复制电话</button></div><div class="seclbl">用户评价 ('+allRevs.length+')</div>'+revHtml;openSheet();}
function openWriteReview(id,name){curDealerId=id;revStars=0;revAvoidTags=[];const nm=document.getElementById('rev-dealer-name');if(nm)nm.textContent='评价 · '+name;document.querySelectorAll('#star-input span').forEach(s=>s.classList.remove('on'));document.querySelectorAll('#avoid-tag-select .df').forEach(d=>d.classList.remove('on'));const rt=document.getElementById('rev-text');if(rt)rt.value='';closeSheet();const rm=document.getElementById('revmod');if(rm){rm.style.display='flex';}}
function closeRevMod(e){const rm=document.getElementById('revmod');if(rm)rm.style.display='none';}
function setStars(n){revStars=n;document.querySelectorAll('#star-input span').forEach((s,i)=>s.classList.toggle('on',i<n));}
function toggleAvoidTag(el,tag){el.classList.toggle('on');if(el.classList.contains('on')){if(!revAvoidTags.includes(tag))revAvoidTags.push(tag);}else{revAvoidTags=revAvoidTags.filter(t=>t!==tag);}}
function submitReview(){if(!revStars){showToast('请先选择评分','red');return;}const text=document.getElementById('rev-text')?.value.trim();if(!text){showToast('请填写评价内容','red');return;}const review={user:curUser?.name||'匿名用户',stars:revStars,text,tags:revAvoidTags.slice(),date:new Date().toISOString().slice(0,7)};saveReview(curDealerId,review);closeRevMod();showToast('✅ 评价已提交！','green');renderDealers();}
function openDealerReg(){document.getElementById('dregmod').classList.add('on');}
function closeDealerReg(e){if(e&&e.target!==document.getElementById('dregmod'))return;document.getElementById('dregmod').classList.remove('on');}
function submitDealerReg(){const name=document.getElementById('dreg-name')?.value.trim();const brand=document.getElementById('dreg-brand')?.value.trim();const checks=['dreg-c1','dreg-c2','dreg-c3','dreg-c4'];if(!name||!brand){showToast('请填写经销商名称和品牌','red');return;}if(!checks.every(id=>document.getElementById(id)?.checked)){showToast('请勾选全部认证承诺','red');return;}lsSet('dealer_app_'+Date.now(),{name,brand,city:document.getElementById('dreg-city')?.value||'',addr:document.getElementById('dreg-addr')?.value||'',phone:document.getElementById('dreg-phone')?.value||'',timestamp:Date.now()});document.getElementById('dregmod').classList.remove('on');showToast('✅ 申请已提交！3-5工作日审核','green');}
const L={zh:{ab:'报价审计',at:'报价单<br><span class="g">极限审计台</span>',as:'Gemini Vision真实OCR · 免费看风险 · Pro解锁Claude 4.6律师话术',ut:'点击上传报价单图片',us:'JPG · PNG · PDF · Gemini Vision真实识别',db:'⚡ 演示 · 加载示例报价单',tb:'TCO 精算',tt2:'全生命周期<br><span class="g">精算中心</span>',ca:'车型 A',cb:'车型 B',pl:'精算参数',py:'持有年限',pk:'年行驶里程',pf:'油价',pe:'电价',chb:'AI 谈判副驾',rb:'价格雷达',rt:'市场行情<br><span class="g">价格雷达</span>',na:'审计',nt:'精算',nc:'副驾',nr:'雷达'},en:{ab:'Quote Audit',at:'Quote<br><span class="g">Extreme Audit</span>',as:'Real Gemini Vision OCR · Free risk view · Pro unlocks Claude 4.6 scripts',ut:'Tap to upload quote image',us:'JPG · PNG · PDF · Real Gemini Vision OCR',db:'⚡ Demo · Load sample quote',tb:'TCO Calc',tt2:'Full Lifecycle<br><span class="g">TCO Center</span>',ca:'Car A',cb:'Car B',pl:'Parameters',py:'Years Owned',pk:'KM / Year',pf:'Fuel Price',pe:'Electricity',chb:'AI Copilot',rb:'Price Radar',rt:'Market<br><span class="g">Price Radar</span>',na:'Audit',nt:'TCO',nc:'Copilot',nr:'Radar'},ja:{ab:'見積審査',at:'見積書<br><span class="g">極限審査台</span>',as:'Gemini Vision OCR · 無料リスク確認 · Pro解錠でClaude 4.6',ut:'タップして見積書アップロード',us:'JPG · PNG · PDF · Gemini Vision',db:'⚡ デモ · サンプル読込',tb:'TCO精算',tt2:'ライフサイクル<br><span class="g">TCO精算台</span>',ca:'車種 A',cb:'車種 B',pl:'精算パラメータ',py:'保有年数',pk:'年間走行距離',pf:'燃料価格',pe:'電気代',chb:'AI交渉コパイロット',rb:'価格レーダー',rt:'市場<br><span class="g">価格レーダー</span>',na:'審査',nt:'精算',nc:'コパイロット',nr:'レーダー'},es:{ab:'Auditoría',at:'Cotización<br><span class="g">Auditoría Extrema</span>',as:'Gemini Vision OCR real · Gratis riesgos · Pro Claude 4.6',ut:'Toca para subir cotización',us:'JPG · PNG · PDF · Gemini Vision OCR',db:'⚡ Demo · Cargar ejemplo',tb:'TCO',tt2:'Ciclo Completo<br><span class="g">Centro TCO</span>',ca:'Auto A',cb:'Auto B',pl:'Parámetros',py:'Años de uso',pk:'KM / Año',pf:'Gasolina',pe:'Electricidad',chb:'Copiloto IA',rb:'Radar',rt:'Mercado<br><span class="g">Radar de Precios</span>',na:'Auditoría',nt:'TCO',nc:'Copiloto',nr:'Radar'}};
function t(k){return (L[lang]||{})[k]||(L.zh||{})[k]||k;}
function setLang(l){lang=l;document.querySelectorAll('.lp').forEach(b=>b.classList.toggle('on',b.textContent.trim()==={zh:'中文',en:'EN',ja:'日本',es:'ES'}[l]));document.querySelectorAll('[data-k]').forEach(el=>{const v=t(el.dataset.k);if(v!==el.dataset.k)el.textContent=v;});document.querySelectorAll('[data-kh]').forEach(el=>{const v=t(el.dataset.kh);if(v!==el.dataset.kh)el.innerHTML=v;});if(curSc==='tco')calcTCO();if(curSc==='radar')renderRadar();if(scanDone&&window._lastAuditData)renderAudit(window._lastAuditData);initChat();}
document.addEventListener('DOMContentLoaded',()=>{const ta=document.getElementById('ci');if(ta){ta.addEventListener('input',function(){this.style.height='auto';this.style.height=Math.min(this.scrollHeight,84)+'px';});ta.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendMsg();}});}const sh=document.getElementById('sheet');let ms=0;if(sh){sh.addEventListener('touchstart',e=>{ms=e.changedTouches[0].screenY;},{passive:true});sh.addEventListener('touchend',e=>{if(e.changedTouches[0].screenY-ms>90)closeSheet({target:document.getElementById('ov')});},{passive:true});}const pc=document.getElementById('paycard');let pm=0;if(pc){pc.addEventListener('touchstart',e=>{pm=e.changedTouches[0].screenY;},{passive:true});pc.addEventListener('touchend',e=>{if(e.changedTouches[0].screenY-pm>80)closePay({target:document.getElementById('paymod')});},{passive:true});}});
function safeRun(fn,name){try{fn();}catch(e){console.warn('Error in '+name+':',e);}}
function boot(){safeRun(initTicker,'initTicker');if(lsGet('pro_guest')==='1')isPro=true;const saved=lsGet('zuser');if(saved&&saved.id){curUser=saved;isPro=isPro||lsGet('pro_'+saved.id)==='1';}safeRun(updateUserUI,'updateUserUI');if(isPro)safeRun(updateProUI,'updateProUI');initClerk().catch(e=>console.warn('Clerk:',e));safeRun(initChat,'initChat');safeRun(calcTCO,'calcTCO');safeRun(calcLoan,'calcLoan');safeRun(renderRadar,'renderRadar');safeRun(renderDealers,'renderDealers');try{const first=document.getElementById('sc-audit');if(first){first.classList.add('on');requestAnimationFrame(()=>requestAnimationFrame(()=>first.classList.add('vis')));}}catch(e){}setTimeout(()=>{try{const sp=document.getElementById('splash');sp.classList.add('out');setTimeout(()=>{try{sp.remove();}catch{}},650);}catch{}},2200);}
boot();
</script>
</body>
</html>
